﻿# Project Roadmap — WinUIRichEditor

`AvaloniaRichEditor` → WinUI 3 (Win2D, unpackaged, Native AOT) 포팅.

> 📜 **개발 이력**(Phase별 구축, 메모리 최적화, 1~7차 전수 리뷰, 파리티 갭 분석, 0.9.0 준비)은
> [`docs/roadmap-archive.md`](docs/roadmap-archive.md)로 옮겼다. 릴리스별 변경은 [`CHANGELOG.md`](CHANGELOG.md).

---

## ✅ 현재 상태 (2026-07-31 · `1.0.0` NuGet 게시, **공개 API 동결**)

1.0을 만든 것은 새 기능이 아니라 **검증 깊이**다. 상류 AvaloniaRichEditor가 1.0을 낸 뒤 그 검증
라운드를 포트에 1:1로 대조하고, 전수조사 4회와 랜덤 편집열 퍼즈까지 돌려 **결함 34건**을 고쳤다.

- 빌드 **0 warn / 0 err**, 헤드리스 테스트 **106/106**
- 공개 표면 **553개**를 `PublicAPI.Shipped.txt`로 동결 — 이제 API 변경은 빌드 경고 + diff로 드러난다
- **Native AOT** 게시·실행·Win2D 렌더 실측(self-contained, 네이티브 exe 15.1MB)
- **NuGet 소비자 스모크 13/13** — 별도 앱이 `PackageReference`로 빌드·실행
- Word · HWP · 브라우저 상호운용 사람 검증

**두 프로젝트의 관계**: 이제 포트-원본이 아니라 **대등한 수렴 peer**다. 양방향으로 개선을 주고받았고,
상류도 1.0이다. 공유 설계(포매터·문서 모델·편집 규칙)를 건드릴 때는 원본을 먼저 대조할 것 —
대조는 **눈대중이 아니라 원본 `PublicAPI.Shipped.txt`와 기계적으로**(이 규칙은 대가를 치르고 얻었다:
눈대중 한 번이 플래그 9개 중 8개를 놓쳤다).

### 다음 우선순위 (1.1)
1. ~~**컨트롤 레벨 자동 검증**~~ → **2026-08-06 착수·1단계 완료**(아래 절). 막고 있다고 본 전제가
   틀렸다 — DP 정적 생성자가 런타임을 **요구하는 것은 맞지만 도달 불가능하지는 않다**.
2. 아래 "알려진 한계"의 미수정 항목들.
3. 용량·메모리·성능은 **2026-08-15 전수조사**(바로 아래)에서 훑었다. 남은 것들은 그 절의
   "측정된 한계"에 근거와 함께 적어 뒀다 — 다시 파기 전에 그것부터 읽을 것.

### AOT 인쇄 대화상자 · 벡터 인쇄 · 툴바 벡터 아이콘 (2026-09-12) — 구현, 테스트 593 → 598, 반증 6종
빌드 0/0, **598/598**(작업 중 OS 클립보드가 막혀 클립보드 경로 11개가 한때 빨갰다 — 풀린 뒤 전부 통과).
`ToolbarVectorIconAndPrintTests` 5. 벡터 인쇄 결과물(글자 선택 가능 여부)과 아이콘 모양은 실기 확인 대기.
- **실기 (2026-09-12, 사용자)**: JIT 데모와 **AOT 게시본**(프로세스 경로로 확인) 둘 다 인쇄 대화상자가 열리고 미리보기가
  문서를 그렸다. 실제 출력(스풀러까지)은 아직 미확인.
- **AOT 인쇄**: `ShowPrintUIAsync`가 AOT에서 `false`로 물러나던 원인은 인쇄 시스템이 아니라 CsWinRT 투영이었다 —
  `ShowPrintUIForWindowAsync`의 결과 `IAsyncOperation<bool>` 변환이 리플렉션을 요구하고, 예외가 대화상자가 뜬 **뒤에**
  났다. `IPrintManagerInterop`을 COM vtable로 직접 부르고 반환 작업도 vtable로 읽는다(`IAsyncInfo.Status` 폴링 →
  `GetResults`). JIT·AOT 한 경로. 라이브러리에 `AllowUnsafeBlocks`(함수 포인터용). 빌드 0/0, 593/593, 데모 AOT 게시
  성공(IL2xxx/IL3xxx 경고 0, 15.5MB).
  - ⚠️ **실기 미확인**: 대화상자·미리보기·실제 인쇄는 자동 검증이 없다. JIT 데모와 AOT 게시본
  (`samples/WinUIRichEditor.Demo/bin/win-x64/publish/WinUIRichEditor.Demo.exe`) 둘 다 사람이 봐야 한다 — AOT는
  "렌더되지만 동등하지 않다"(`LineMetrics` 전례).
  - 데모 툴바 페이지 인쇄: PDF 직행 → "대화상자 먼저, 안 되면 PDF"(보기 페이지와 같게).
- **벡터 인쇄**: 실기에서 드러난 한계 — 대화상자는 뜨지만 결과가 **150DPI 그림**이었다(PDF 프린터 출력에 글자 없음).
  Win2D `CanvasPrintDocument`로 바꿔 렌더러가 프린터 세션에 직접 그린다. 페이지 그리기를 `DrawPrintPage`로 떼어 호출자의
  변환 **위에 합성**하게 했다(용지 맞춤 축소가 그 위에 올라간다) — `ThePrintPageDrawer_…` 테스트가 고정. 인쇄 이벤트가
  UI 스레드 밖에서 오면 지연(deferral)을 쥐고 UI 스레드로 옮긴다. `SavePdf`는 여전히 이미지 PDF(글꼴 내장 PDF 작성은 별도 큰 일).
- **아이콘**: 처음엔 Segoe `E898`/`E896`으로 바꿨으나 실기에서 상류 그림과 **다른 모양**(막대 화살표)이었다 → 툴바 17개를
  상류의 경로 그림으로 직접 그린다(`ToolbarIcons.CreateVector`, 파서 `PathMarkup`). B/I/U/S는 모양 준 글자. 메뉴는 Segoe 유지.
  - ⚠️ 첫 판의 비활성 흐림은 `IsEnabledChanged`에 걸었다가 테스트가 잡았다 — 로컬 값 설정에 동기로 오지 않는다(트리 밖에선
    아예 안 온다). `RegisterPropertyChangedCallback(IsEnabledProperty)`로.

### 감사: 캐럿 서식 보고 (2026-09-12) — 완료, 테스트 571 → 593, 결함 4건 + 같은 뿌리 2 + 동작 결정 3
"관찰이 0인 공개 멤버" 스캔의 다섯 덩어리 중 하나. `CaretFormat`의 `Align`·`Heading`·`Quote`·`Strike`와 `SetFontFamily`
등이 테스트 참조 0이었다. 검사 기준 — **오라클**: 캐럿 보고는 툴바가 "다음 타이핑이 쓸 서식"과 "화면"에 대해 하는
주장이다. 그러니 캐럿 위치마다 보고 = 거기서 친 글자의 서식이어야 한다.
- **실측(일회용 프로브)**: 이미지 옆 4개 위치에서 보고 ≠ 타이핑(보고는 문단 **마지막** run, 타이핑은 서식 없는 새 run) ·
  굵은 제목이 "굵게 아님" · 링크의 Ctrl+U가 화면 변화 없이 숨은 플래그만 뒤집음 · 링크 끝 타이핑이 링크를 늘림 ·
  호스트 기본 크기 14에서 제목 속 `ClearFormatting`이 H1을 20 → 14로.
- **수정**: `TypingSource` 한 규칙을 삽입·보고·캐럿 높이·서식 복사가 공유한다. `RunAtOffset`의 "마지막 run" 폴백을
  없앴다 — 이것이 `CurrentLinkUri`(이미지 옆에서 먼 링크 반환)와 이미지 앞 캐럿 높이의 뿌리이기도 했다. 강제 서식은
  `DrawnBold`/`DrawnUnderline`/`DrawnForeground`로 렌더러와 보고가 공유한다(`DrawnRunSize`와 같은 짝, 규칙 1).
- **테스트**: `ControlCaretFormatTests` 21(12개 레이아웃 × 모든 캐럿 위치 스윕 · 이미지 옆 규칙 · 링크 경계·이음매 ·
  `CurrentLinkUri` · 강제 서식 토글 · `ClearFormatting` · 보류 서식 미리보기) + `ControlCaretHeadingTests`에 이미지 앞 캐럿 높이 1.
- **반증 15종 — 전부 의도한 테스트만 실패**: (15번째는 상류 백포트 PR #28에서 되돌려 받은 것 — 캐럿 단어를 타이핑
  서식으로 판단하는 교란을 잡는 테스트가 양쪽에 없었다. `CtrlU_AtALinksEnd_LeavesTheLinkAlone`가 잡는다.) 삽입·보고 각각 `TypingSource` 무시 · `RunAtOffset` 마지막 run 폴백 복원 ·
  캐럿 높이 폴백 제거 · 제목 굵게 run 보고 · 굵게/밑줄 강제 판정 제거(각각) · 링크 항상 연장 · 링크 시작에서 합류 ·
  `ClearFormatting`이 `DefaultFontSize` · 미리보기 복제본 부모 없음 · 링크 색 미보고 · 쪼개진 링크 이음매 무시 ·
  "하나라도 강제면 멈춤"(`All`→`Any`).
  ⚠️ 첫 12종으로는 **섞인 선택 테스트 둘**(제목+본문 Ctrl+B, 링크+본문 Ctrl+U)과 링크 시작 경로가 어느 교란에도 죽지
  않았다 — 그 둘이 지키는 것은 "섞여 있으면 멈추지 않는다"이므로 `All`→`Any` 교란을 더해 확인했다.
- ⚠️ **상류도 같은 뿌리일 가능성**(정독, 미측정): 상류 `GetCaretFormat`은 이미지에서 문단 **첫** run으로 폴백하고, 삽입 규칙은
  포트의 옛 것과 같으며, 제목 굵게를 강제하면서 run 굵기를 보고한다. 다음에 측정할 후보.

### 감사: 제목 속 캐럿 크기 (2026-09-12) — 완료, 테스트 568 → 571, 결함 1건
제목 적용 시 직접 지정한 크기를 유지하기로 한 결정(결정 사항 절)의 뒷면이다. 렌더러는 그런 run을 **자기 크기로**
그리는데(`DrawnRunSize`), 캐럿 높이·baseline이 쓰는 `RunFontSizeAt`만 제목 안의 모든 run에 **제목 크기**를 돌려줬다.
- **실측(수정 전, H1)**: 직접 지정한 36pt run의 캐럿 32.0 — 본문의 같은 run은 57.6. 12pt run의 캐럿도 32.0으로, 자기
  줄 상자(21.3)보다 컸다. 무서식 run(→20pt)은 원래 맞았다.
- **수정**: `RunFontSizeAt`이 렌더러의 규칙 `DrawnRunSize`를 쓴다 — 캐럿 서식이 이미 공유하던 그 헬퍼(규칙 1).
- **테스트** `ControlCaretHeadingTests` 3 — 공식이 아니라 **기준과 비교**: 제목 속 run의 캐럿 = 본문 속 같은 run의
  캐럿, 그리고 캐럿 ≤ 줄 상자. 무서식 run이 제목 크기를 받는 경우도 고정.
- **반증 2종**: 옛 동작(제목 크기 우선) → 36/12pt 두 케이스만 옛 측정값 32.0으로 실패. run 크기만 보기(제목 무시) →
  무서식 케이스만 실패(16.0 vs 32.0).
- ⚠️ **상류는 반대 방향이었다 — 측정 후 수정, 상류 PR #27**(#26 위): 상류 `CaretTextHeight`는 제목을 보지 않고
  저장된 run 크기만 써서 무서식 제목에 10pt 캐럿이 섰다(실측 H1 19.0 vs 본문 20pt 29.1). 본문에선 캐럿이 줄
  상자로 잘려 안 보였다. 같은 클램프 때문에 제목보다 **작은** 명시 크기로는 판별이 안 되어(12pt 케이스가 공허했다)
  상류 테스트는 28pt를 쓴다. 포트의 12pt 케이스는 포트 캐럿에 그런 클램프가 없어 판별된다(반증에서 실패 확인).

### 상류 백포트 묶음 (2026-09-12) — 완료, 상류 PR #26 (`890849d`)
아래 네 라운드(편집 명령 · 기능 플래그 · 문서 API 래퍼 · 하이퍼링크)의 "상류 백포트 후보"를 상류에 옮겼다.
상류 테스트 **889 → 915**, 반증 18종 전부 의도한 테스트만 실패. 각 절의 후보 표시는 완료로 바꿨다.
- **옮긴 것(#26)**: 캐럿 서식의 크기 보고(`DrawnRunSize`) · 로드 시 서식 복사 해제 · Tab 자동 링크 · 괄호 짝
  (`TrimUrlTail`) · JSON/패키지 형태 검사 · `document.json` 없는 zip 거부 · 페이지 설정 호스트 기본값 ·
  붙여넣기·`InsertHtml`의 `AllowImages`/`AllowTables` 적응.
- **이미 상류에 있던 것**: Word 방식 토글 · 병합 셀 행/열 삽입 위치 — 상류 `c90cda5`(PR #25)가 먼저 옮겼다.
- ⚠️ **두 리포가 갈라진 곳**: 제목 적용 시 run 크기 — 상류는 **본문 기본값으로 초기화**(`c90cda5`), 포트는 사용자
  결정으로 **직접 지정한 크기를 유지**(결정 사항 절). 묶음은 이 차이를 건드리지 않았다.
- 상류 쪽 알려진 공백: 상류 스위트엔 실제 클립보드가 없어 붙여넣기 세 지점의 배선은 검증 밖(적응 로직은 `InsertHtml`로).

### 감사: 하이퍼링크 (2026-09-12) — 완료, 테스트 530 → 568, 결함 4건
빌드 0/0, **568/568**(작업 중 OS 클립보드가 다시 막혀 클립보드 경로 11개가 한때 빨갰다 — 풀린 뒤 전부 통과). 상세는 CHANGELOG.

같은 기준의 네 번째 축. `AutoLinkOnType`·`OpenLinkAtCaretAsync`·`EditHyperlinkAsync`와 클릭 경로가 테스트 참조 0이었다.

- [x] `ControlHyperlinkTests` 38 — 상류 `Round2BackportTests`의 자동 링크 3 이식 + 자동 링크 경계 · 실행 허용 판정
      (`ms-msdt:`·`search-ms:`·`ms-settings:`·`file:`·경로·`javascript:`·`mailto:` 거부) · 메뉴 "링크 열기" 활성 상태 ·
      **창에 호스팅한 에디터에서 클릭 경계**(링크 양끝 안팎, 문단 끝·시작, 자동 줄바꿈 줄 끝, 링크 자체의 줄바꿈, 문단 사이 틈).
- **런처는 테스트에서 부르지 않는다** — 실행이 걸린 판정(`IsLaunchableLink`)과 클릭 대상 판정(`LinkAtPoint`)을 시험한다.

#### 결함 4건 (전부 상류와 대조)
- **문서 속 링크를 스킴 검사 없이 실행했다(보안, 포트만)** — `OpenUriAsync`가 `Uri.TryCreate`만 보고 **아무 스킴이나**
  Windows 런처에 넘겼다. 링크 주소는 HTML `href`·RTF `HYPERLINK`에서 값 그대로 들어오므로, 붙여넣은 페이지나 받은 파일의
  `ms-msdt:`(Follina 핸들러)·`search-ms:`·`ms-settings:`·앱 전용 프로토콜이 Ctrl+클릭 한 번 — 읽기 전용 뷰어에서는 일반 클릭
  한 번 — 에 확인 없이 실행됐다. **상류는 이미 http/https만 연다**(`OpenUrl`) — 같은 규칙으로 수렴했다(`mailto:`도 열지
  않는다, 상류와 동일). 컨텍스트 메뉴의 "링크 열기"는 열 수 없는 링크에서 비활성.
- **링크 클릭 판정이 반 글자 어긋났다(포트만)** — 호버·Ctrl+클릭·읽기 전용 클릭이 "캐럿 **앞** 글자의 런"을 봤는데,
  클릭이 만드는 캐럿은 **가장 가까운 경계**다. 실측: 링크 첫 글자 1.5px 안 → 링크 아님, 링크 끝 1.5px **밖** → 링크.
  상류는 문자를 히트테스트한다(`IsInside`). `LinkRunAtPoint`가 캐럿이 나온 **같은 레이아웃**에서 포인터 아래 글자를 구한다
  (포인터가 캐럿 왼쪽이면 앞 글자, 오른쪽이면 캐럿의 글자, 그 글자가 다른 줄이면 없음 — 규칙 #1 유지).
- **Tab 키가 자동 링크를 걸지 않았다** — `AutoLinkOnType` 계약은 공백·탭·Enter인데, Tab은 공백 넷을 넣고 `InsertText`는
  한 글자 공백만 자동 링크한다. 같은 언두 그룹 안에서 링크를 건다.
- **괄호로 끝나는 URL이 잘렸다(공유)** — 위키백과 `…/Foo_(bar)`가 `…/Foo_(bar`로 링크됐다(다른 페이지). 닫는 괄호는
  **짝이 모자랄 때만** 뗀다(`TrimUrlTail`) — `(see https://x.com)`의 `)`는 여전히 떨어진다.

**그대로 옳았던 것(실측)**: Enter · 괄호 속 URL · 끝의 마침표/쉼표 · 쿼리·조각. `localhost`(점 없는 호스트)·`mailto`는
의도대로 링크하지 않는다. Undo 한 번에 공백과 링크가 **함께** 사라진다(상류와 같다 — Word는 링크만 먼저 되돌린다. 고정만 함).

#### 반증 — 8종 전부 의도한 테스트만 죽었다
스킴 검사 제거 · 메뉴 항상 활성 · 닫는 괄호 무조건 제거 · Tab 자동 링크 제거 · 옛 offset-1 판정 · 새 판정의 가드 셋(줄 끝 /
세로 위치 / 줄 시작) 각각.
> **H7·H8(세로 위치·줄 시작 가드)은 처음에 못 잡았다** — 가드가 공허해서가 아니라 **테스트가 그 경우에 닿지 못해서**였다:
> 문단 끝·시작에서는 다른 조건이 먼저 답한다. 도달 가능한 경우 — **링크 자체가 줄바꿈될 때 두 번째 줄 왼쪽 여백**,
> **두 문단 사이 틈** — 를 테스트로 만들자 잡혔다. (H6 패턴은 `'\n'` 따옴표 문제로 두 번 미적용 — 스크립트 문제.)

#### 알려진 공백
- 실제 실행 배선(`OpenUriAsync` → 런처)과 Ctrl+클릭 이벤트 경로는 자동 검증 밖이다(런처를 부를 수 없고 포인터를 주입할 수 없다).
- 텍스트 메뉴 쪽 "링크 열기"의 활성 상태는 테스트가 없다 — 링크 메뉴(`BuildLinkMenu`)만 고정했다.

#### 상류 백포트 — ✅ 완료 (상류 PR #26)
- `TrimUrlTail`(괄호 짝)과 **Tab 자동 링크** — 상류 `TryAutoLink`/`HandleTab`가 글자 그대로 같았다. (스킴 허용 목록과
  문자 히트테스트는 **상류가 이미 나았고 포트가 수렴**한 것.)

### 감사: 문서 API 래퍼 (2026-09-12) — 완료, 테스트 506 → 530, 결함 1건 + 동작 결정 3
빌드 0/0, **530/530**. 상세는 CHANGELOG.

같은 기준의 세 번째 축. `LoadJson`·`LoadJsonAsync`·`ToJson`·`ToJsonAsync`·`SavePackageAsync`·`LoadPackageAsync`·
`ToRtf`·`DocumentChanged`가 **테스트 참조 0**이었다. 포매터 자체는 왕복·모델 퍼즈로 두텁게 검증되지만, 그 위의 래퍼 —
**사용자 파일을 지키는 계약**(문서가 아닌 입력은 보고하고, 열린 문서는 그대로 둔다) — 은 `LoadRtf` 하나만 검증돼 있었다.

- [x] `ControlDocumentApiTests` 24 — 상류 `RichEditorControlTests`의 JSON 왕복 4 이식 + 패키지 왕복, 그리고 거부될 때
      열린 문서가 **같은 인스턴스·같은 텍스트·언두·수정 표시까지** 그대로인지(JSON 5 + 비동기 1 + 패키지 5), 로드가
      초기화하는 것(이력·수정 표시·`DocumentChanged` 1회·서식 복사·대기 서식), 페이지 설정의 출처(아래).

#### 실측 (일회용 프로브 — 가설 2개 반증, 3개 확인)
- **계약대로였던 것**: 배열·빈 문자열·잘린 JSON → `JsonException`, 무작위 바이트·**잘린 패키지**·빈 스트림 →
  `InvalidDataException`, 모두 열린 문서 유지. 잘린 zip이 문서화되지 않은 예외를 낼 거라던 가설은 틀렸다.
- **열린 문서를 날린 것**: `{}`·**다른 앱의 JSON**(`package.json` 모양)과 **`document.json` 없는 zip**(`.docx` 모양)은
  예외 없이 **빈 문서로 교체 + 언두 삭제 + "저장됨" 표시** — 다음 저장이 원본을 빈 문서로 덮어쓴다. 상류도 같은 코드다.
- **새는 것**: 서식 복사가 로드 뒤에도 켜진 채였고, 페이지 설정이 **직전 문서에서 다음 문서로** 넘어갔다.

#### 동작 결정 3건 (사용자, 2026-09-12)
- **우리 형식이 아닌 JSON은 거부** — `Blocks`가 없는 객체는 `JsonException`. 우리 직렬화기는 빈 문서에도 `Blocks`를 쓰므로
  이것이 표식이다(DTO 기본값을 null로 바꿔 "없음"과 "비어 있음"을 구분). 리터럴 `null`은 문서화된 대로 빈 문서.
  **에디터 경로(`LoadJson`/`LoadJsonAsync`/`LoadPackageAsync`)만** 엄격하다 — 공개 `DocumentSerializer.Deserialize`는 관대한 채다.
- **`document.json` 없는 zip은 거부** — `DocumentPackage` 수준에서 `InvalidDataException`(선택지대로 "이미지만 든 컨테이너도
  연다"는 주석을 정정). 우리 `Save`는 항상 `document.json`을 쓴다.
- **페이지 설정은 호스트 기본값에서 시작** — 설정 없는 문서와 새 문서(`Clear`)는 호스트가 코드로 정한 값에서 시작하고, 직전
  문서의 값은 넘어오지 않는다. 문서에서 적용된 값(`_syncingPageSetup`)과 **툴바 용지·방향 콤보**(`EditDocumentPageSetup`
  — 열린 문서의 편집)는 호스트 기본값이 아니다.
  > ⚠ **속성 단위로 기록한다.** 페이지 DP의 콜백은 `DefaultFontSize` 같은 무관한 DP와 공유된다 — 거기서 모든 페이지
  > DP를 스냅샷하면 A5 문서를 연 채 호스트가 글꼴 크기만 바꿔도 A5가 호스트 기본값이 된다. 반증 D10이 그 판을 잡는다.

#### 결함 1건
- **서식 복사가 문서 교체를 넘어 켜진 채였다** — 새 문서의 다음 선택에 **이전 문서의 서식**이 칠해진다. 문서 교체
  (`OnDocumentAssigned`)에서 서식 복사와 대기 서식을 함께 지운다(상류 `ResetInteractionState`는 대기 서식을 지운다).
  > 대기 서식은 프로브에서 **넘어오지 않았는데**, 로드가 지운 게 아니라 프로브의 `FocusDocumentEnd`(캐럿 이동)가 지운
  > 것이었다. 테스트는 캐럿을 움직이지 않고 로드 직후 입력한다 — 움직이면 초기화를 가린다.

#### 반증 — 12종 전부 의도한 테스트만 죽었다
형태 검사 우회(`LoadJson`/`LoadJsonAsync`/패키지 각각) · 형태 검사 무력화 · DTO 기본값을 `new()`로(신호 설계 자체) ·
`document.json` 없는 zip 허용 · 서식 복사/대기 서식 초기화 제거 · 현재값 채택으로 되돌림 · 스냅샷 기록 · 툴바의 "문서
편집" 표시 제거 · 호스트 기본값 기록 제거.
> **반증 스크립트의 함정 두 가지(제품이 아니라 스크립트)**: ① 소스가 **CRLF**라 `\n`으로 쓴 perl 패턴 3개가 적용되지
> 않았다(`\r?\n`으로). ② 테스트 이름 추출 정규식이 인자 속 괄호(`(a .docx, say)`)에서 끊겨 "잡혔는데 이름 없음"으로
> 보였다 — 출력 원문에는 의도한 테스트의 실패가 그대로 있었다. **"PATCH DID NOT APPLY"와 "이름 없음"은 결과가 아니다.**

#### 상류 백포트 — ✅ 완료 (상류 PR #26)
- 에디터 로드 경로의 JSON 형태 검사 · `document.json` 없는 zip 거부 · 페이지 설정 호스트 기본값 · 로드 시 서식 복사 해제.
  상류는 비동기 로더가 모델을 UI 스레드에서 만들어(규칙 #8) 형태 검사를 파싱 직후 `FromDto` 전에 넣었다. 상류 테스트
  `FlowPackage_ZipWithoutDocument_ReturnsAnEmptyDocument`는 `_Throws`로 뒤집었다.

#### 다음 (같은 기준)
- 하이퍼링크(`AutoLinkOnType` 경계) — ✅ 완료 · 제목 안 명시 크기 런의 캐럿 크기 · 상류 백포트 묶음 — ✅ 완료(#26).

### 감사: 기능 플래그 게이트 (2026-09-12) — 완료, 테스트 461 → 506, 결함 4건 + 동작 결정 1
빌드 0/0, **506/506**. 상세는 CHANGELOG.

같은 기준(공개 멤버 참조 0)의 두 번째 축. `Allow*` 7종이 코드 약 63곳에서 분기하는데 테스트 참조가 0이었다 —
게이트가 막지 못해도 **증상이 없는** 축이다(기능이 계속 동작할 뿐). 플래그 표면은 상류 `PublicAPI.Shipped.txt`와
기계적으로 동일했다.

- [x] `ControlFeatureFlagTests` 45 — 상류 `FeatureFlagTests` 8 + `ImageLimitTests` 5 이식, 그리고 양쪽 다 0이던 것:
      이미지 삽입 진입점 3(+대조군 3) · 찾기 단축키(+대조군) · **개인정보 게이트를 세어서 판정** · 툴바 반영 · 붙여넣기 적응.
- [x] **개인정보 게이트는 원래 옳았다(측정).** 원격 이미지: 루프백 `TcpListener`로 **접속 수**를 셌다 —
      `LoadHtmlAsync`와 **실제 클립보드 붙여넣기** 모두 플래그를 끄면 0회, 켜면 1회 이상(대조군 — 이게 없으면 "이미지 없음"은
      요청했다가 실패한 것과 구분되지 않는다). 로컬 파일: TEMP 밖 PNG는 막힌다.

#### 결함 4건 (전부 상류와 대조)
- **블록 이미지 삽입이 `AllowImages`를 보지 않았다** — `InsertImageBlock`과 그 별칭 `InsertImageBytes`. 인라인 판만 봤다
  (쌍의 반쪽). 붙여넣기는 **호출부가** 먼저 거르므로 가려져 있었고, 공개 API로는 이미지가 그대로 들어갔다. 상류는 막는다.
- **`AllowLocalFileImages=false`여도 `LoadHtml`/`InsertHtml`이 %TEMP% 파일을 읽었다** — 포트가 Word/HWP 붙여넣기용으로
  넣은 TEMP 예외가 파서의 **모든 호출자**에 적용됐다(다른 앱의 임시 파일 포함). 공개 `ParseHtml`은 예외를 유지하고(기존
  테스트가 고정, 호스트가 의존할 수 있다) 주석에 명시했다. 에디터 경로만 내부 오버로드로 엄격하게 했다. 상류는 예외 자체가 없다.
- **툴바가 플래그 변경을 다음 입력 때까지 반영하지 않았다** — 플래그 DP에 변경 콜백이 없고 툴바는 `StatusChanged`에서만
  다시 읽는다. 이제 세 플래그가 바뀌면 `StatusChanged`를 올린다. 상류 툴바는 속성 변경을 직접 구독한다.
- **구분선 버튼이 플래그를 따르지 않았다** — 표·이미지를 모두 꺼도 보였다(컨텍스트 메뉴·상류 툴바는 숨긴다).

#### 동작 결정 — 붙여넣기와 `InsertHtml`에 섞인 이미지·표 (사용자 결정, 2026-09-12)
**측정**: 이미지·표를 끈 에디터에 HTML을 넣으면 인라인 이미지 · 블록 이미지 · 표 · 셀 속 이미지 **넷 다 그대로 들어왔다** —
리치 붙여넣기는 `AllowRichPaste`만 봤다. 결정: **이미지는 버리고, 표는 셀 순서대로 풀어 문단으로**(빈 셀은 빈 줄을 만들지 않고,
인라인 표는 셀 글자를 공백으로 잇는다).
- `AdaptToCapabilities`를 **네 지점**(앱 내부 스냅샷 · RTF · HTML 붙여넣기 · `InsertHtml`)에서 부른다. 공통 삽입부
  `InsertDocumentAtCaret` 안에 넣지 **않았다** — `DragText`도 거기로 들어오는데, 이미 문서에 있는 그림을 끌어 옮기는 순간
  지우게 된다. 문서 열기(`Load*`)도 적응하지 않는다(플래그는 **추가**를 막지, 기존 문서를 막지 않는다).
- 걸러서 아무것도 안 남으면 삽입하지 않는다 — 빈 언두 단계도 수정 표시도 없다.
- 상류는 앱 내부 **인라인** 붙여넣기에서 인라인 이미지만 버렸다 — ✅ **상류 PR #26으로 수렴 완료**(외부 붙여넣기는
  `InsertParsedDocument` 한 곳에서, 앱 내부 붙여넣기는 보관 목록의 복제본에 적응).

#### 반증
- 클립보드 무관 **9종, 전부 의도한 테스트만 죽었다**: 블록 이미지 게이트 · `InsertHtml` 적응 · 블록 이미지 규칙 · 인라인 이미지
  규칙 · 표 풀기 규칙 · TEMP 조건 · 플래그 알림 · 구분선 · 빈 결과 처리.
  > ⚠ **"빈 결과" 테스트의 첫 판은 공허했다** — 큰 그림(블록으로 파싱)만 넣었는데, 걸러내면 블록 목록 자체가 비어 기존
  > `Blocks.Count == 0` 검사가 먼저 걸린다. `emptied` 검사를 지워도 초록이었다. 빈 문단이 남는 **작은 인라인 그림**
  > 모양을 더하자 잡혔다.
- 클립보드 경로 **4종도 전부 의도한 테스트만 죽었다**: 붙여넣기 세 지점(스냅샷·RTF·HTML)의 적응 제거 — 지점마다
  자기 테스트 하나씩 — · 인라인 표 풀기 규칙(앱 내부 복사 테스트).

#### OS 클립보드가 막혔던 것 (2026-09-12, 해소)
실행 도중 모든 클립보드 쓰기가 실패했다 — 앞서 통과한 **기존 `ControlClipboardTests` 중 쓰는 테스트 6개**(이번 변경과
무관한 복사 전용 포함)가 실패하고 PowerShell `Set-Clipboard`도 실패했다. 사용자가 클립보드를 풀자 14/14로 돌아왔다.
> **다음에 클립보드 테스트가 한꺼번에 빨개지면** 코드보다 먼저 이 순서로 볼 것: ① 무관한 **복사 전용** 테스트도 실패하는가
> ② PowerShell `Set-Clipboard`/`Get-Clipboard`가 되는가(되지 않으면 환경) ③ 클립보드를 쓰는 앱(휴대폰과 연결·클립보드
> 관리자·원격 데스크톱). 시스템 서비스는 건드리지 말고 사용자에게 알린다.

#### 알려진 공백
- `DragText`가 적응을 **거치지 않는다**는 규칙은 테스트가 없다 — 드래그는 포인터 주입 없이 구동할 수 없다(로드맵 "포인터/키 배선").

#### 다음 (같은 기준)
- 문서 API 래퍼(`LoadJson`·`LoadHtml`·패키지)의 손상 입력 계약 · 하이퍼링크(`AutoLinkOnType`) · 제목 안 명시 크기 런의 캐럿 크기.

### 감사: 편집 명령 축 (2026-09-11) — 완료, 테스트 413 → 461, 결함 4건 + 동작 변경 1
빌드 0/0. 명령 퍼즈 **2000시드 클린**(표·크기 수정 후, 10분 35초), 이후 변경(Word 토글·제목 크기)까지
넣고 **400시드 클린**. 상세는 CHANGELOG.

**같은 방법(관찰이 0인 축)을 이번엔 공개 표면 전체에 기계적으로 돌렸다.** `PublicAPI.Shipped.txt`의 멤버
이름을 `tests/` 전체와 대조하니 **`RichEditor` 공개 멤버 107개가 테스트 참조 0**이었다. 가장 큰 덩어리는
서식 명령 약 25개였고, 이유는 구조적이었다 — **모델 퍼즈는 `FlowDocument`를 직접 편집하고 `RichEditor`
명령을 한 번도 거치지 않는다**(런타임을 못 띄우던 시절에 쓰였다). 상류 퍼즈는 명령을 구동한다.

- [x] `ControlCommandFuzzTests` — 명령 약 40종(키 동작·서식·목록·표 구조·삽입·언두/리두)을 `UiThread`
      위에서 랜덤 구동하고 매 단계 오라클 셋을 본다: ① **구조**(모델 불변식 + 정규화 규칙 #5 + Parent 체인 +
      캐럿·선택 도달성) ② **이력** — 문서를 바꾼 단계는 정확히 한 언두 단계다(Undo가 직전 모양, Redo가
      직후 모양) ③ **변경 추적** — `IsModified`와 `TextChanged`. CI 24시드 3초. `RICHEDITOR_CMD_FUZZ_SEEDS`로
      넓히고 `RICHEDITOR_CMD_FUZZ_ONLY=746,1035`로 특정 시드만 돌린다(2000시드 8분 → 1초).
- [x] `ControlFormattingCommandTests` 41 — 상류 `ParagraphCommandScopeTests` 이식 11 + 목록 토글의 인라인
      객체 **동일성** 2(상류 퍼즈의 발견) + 양쪽 다 0이던 것(캐럿 단어 대상 · 대기 서식 · 서식 복사 · 셀
      사각형 · 크기 사다리 · 하이퍼링크 · 읽기 전용이 23개 명령을 전부 거부) + Word 토글 규칙 13(아래).
- [x] `ControlTableCommandTests` 6 — 아래 결함 둘(캐럿이 피복 칸 · 병합 셀의 삽입 위치).
- [x] **반증.** 퍼즈 교란 7개 중 6개를 **24시드 안에** 잡았다: 체크포인트를 변경 뒤로 22/24 ·
      `SetModified` 제거 24/24 · `TextChanged` 제거 24/24 · 블록 삽입이 꼬리 문단 누락 24/24 · 정렬
      체크포인트 제거 21/24 · 인접 비문단 정규화 제거 3/24(**단 시드 문서 대입에서만** — 명령이 인접 상태를
      만든 적은 없다). 결정적 테스트 교란 9개는 **전부 의도한 테스트만** 죽었다.
      > **못 잡은 1개가 결정적 테스트를 낳았다.** 목록 토글이 인라인 객체를 **복제**해도 퍼즈는 초록이다 —
      > 복제본은 모양이 같고, 포트는 캐럿이 셀 안이면 셀 분기로 가서 호스트를 쪼개지 않으므로 분리된
      > 사본을 가리키는 포인터도 생기지 않는다. 이 결함은 **객체 동일성으로만** 보인다(상류가 전용
      > 테스트로 고정한 이유). 동일성 테스트 2개가 잡는다.

#### 결함 3건 (전부 상류와 대조함)
- **표 행/열 명령이 캐럿을 피복 칸에 둔다** — 세로 병합 셀에서 "아래에 행 삽입", 가로 병합 셀에서
  "오른쪽에 열 삽입", 병합 옆 행/열 삭제. 네 명령이 캐럿을 고정 슬롯(행 `at`·열 0 / 행 0·열 `at`)에
  두는데, 병합 안에 삽입하면 병합이 새 칸을 덮고 삭제는 피복 칸으로 clamp될 수 있다. 캐럿이 **그려지지도
  저장되지도 않는 칸**에 들어가고 거기 입력한 글자는 사라진다. **2000시드에서만** 나왔다(3시드, 전부
  CI 24 밖). **상류는 이미 `CellCaretTarget`으로 고쳐 두었다** — 이름·본문 그대로 옮겼다. 퍼즈는 셋을
  찾았고 열 삭제는 같은 모양이라 함께 고정했다(쌍의 반쪽).
  > ⚠ **감사 2.5의 기각 전제가 틀렸었다** — "피복 셀에는 캐럿이 들어갈 수 없다"는 이 네 명령에서
  > 거짓이었다. 수정 후 다시 참이 되어 기각 결론은 유지되지만, 이제는 테스트가 그 전제를 지킨다.
- **미지정 크기(`FontSize ≤ 0`) 텍스트의 캐럿 서식이 `DefaultFontSize`가 아니라 10** — 호스트가
  `DefaultFontSize=14`면 툴바는 10을 표시하고 "크게"가 14pt 글자를 **10.5로 줄였다**. 렌더
  (`CreateLayout`)와 캐럿(`RunFontSizeAt`)은 `DefaultFontSize`, `GetCaretFormat`만 `BodyFontSizePt`였다 —
  `DefaultFontSize`의 문서 계약("툴바의 표시 폴백 크기")을 한 줄이 어겼다. 사다리 테스트를 쓰다 의심해
  **일회용 프로브로 확인한 뒤** 고쳤다(`runFontSize=0 toolbarShows=10 caretBasis=14 afterIncrease=10.5`).
- **제목 안의 서식 없는 런도 같은 모양** — H1(20pt로 그려짐)에서 툴바는 10을 표시하고 "크게"가 제목을
  **10.5로 줄였다**(`H1 drawnBefore=20 toolbarShows=10 → drawnAfter=10.5`). 위 수정 뒤 "보고 크기가 그린
  크기와 다를 수 있는 곳이 또 있나"를 물어 프로브로 찾았다. 땜질 대신 **렌더러의 크기 규칙을
  `DrawnRunSize` 하나로 뽑아** `CreateLayout`과 `GetCaretFormat`이 함께 쓴다 — 툴바가 그려지지 않는
  크기를 보고할 수 없는 구조다(위 `DefaultFontSize` 수정도 여기에 흡수).
  > **반증이 드러낸 것: 렌더러의 제목 크기 규칙은 테스트가 0이었다.** `DrawnRunSize`에서 제목 분기를
  > 끄면(제목이 본문 크기로 그려짐) 458개 중 **새 툴바 테스트 하나만** 실패한다. 공유 헬퍼 덕에 이제
  > 그 테스트가 렌더 규칙까지 지킨다 — 통합하지 않았다면 여전히 무방비였다.

#### 실기 확인 (2026-09-12) — WinUI 데모에서 3개 전부 통과
첫 확인에서 **체크리스트 3개가 전부 "의도대로 안 된다"** 로 돌아왔는데, 사용자가 **AvaloniaRichEditor 앱을
보고 있었다**(혼동). 세 증상은 전부 **상류의 현재 동작**이다 — 상류는 여전히 런마다 뒤집고, 메뉴는
`r + 1`이고, 명시 크기는 제목을 따르지 않는다. 새로 빌드한 WinUI 데모로 다시 보니 셋 다 의도대로 동작했다.
그래도 두 가지는 남는다.
- **WinUI 데모도 옛 라이브러리였다**(출력 DLL이 9/6·9/8 빌드) — 테스트 프로젝트만 빌드하고 **데모를
  재빌드하지 않은 채** 확인을 요청했다. 이번엔 관찰이 상류 앱에서 나왔지만, WinUI 데모를 봤어도 옛 동작을
  봤을 것이다. 로드맵이 이미 경고한 함정("데모 빌드 순서", 재사용 레시피)이다. **실기 확인을 요청하기
  전에 `slnx` → 데모 빌드 후 데모 출력 폴더의 `WinUIRichEditor.dll` 타임스탬프를 볼 것.**
- **3번 증상은 포트에도 있는 진짜 결함이었다**(코드가 같다) — 세로 병합 셀에서 "아래에 행 삽입"이 병합
  **아래가 아니라 안쪽**에 행을 넣었다. 메뉴가 `r + 1`을 넘기는데, 병합 안의 행 삽입은 병합을 늘리므로 새 칸이 병합 **옆 열에만**
  생긴다. Word처럼 병합 전체 뒤(`RowBelowIndex` = `r + 행 병합 수`, `ColumnRightIndex`도 같은 모양)로
  고쳤다. 테스트 2, 반증 확인(`+1`로 되돌리면 각각 실패). 이 감사의 캐럿 수정과는 **다른 결함**이다 —
  그쪽은 "캐럿이 어디로 가는가", 이쪽은 "행이 어디에 들어가는가". 병합 안 삽입은 병합 옆 칸의
  "위에 행 삽입"으로 여전히 도달 가능해서, 캐럿 테스트 둘을 그 경로로 옮겼다.
- **2번(제목 1→2에서 크기 불변)은 설계대로였다** — 체크리스트 2번의 "크게"가 **명시 크기**를 박았고,
  명시 크기는 제목 수준을 따르지 않는다. 서식 없는 글자는 따른다. 둘 다 테스트로 고정했고, **이 설계를
  유지하기로 결정했다**(사용자 — 아래 "결정 사항" 절).
  > ⚠ 그 테스트의 첫 판은 **사다리에서 16 다음을 20으로** 잘못 적었다(실제 18) — 제품이 아니라 테스트가
  > 틀렸고, 메시지를 먼저 읽어 갈랐다.

#### 상류 백포트 — ✅ 완료
- **병합 셀의 "아래에 행 삽입"/"오른쪽에 열 삽입"** — ✅ 상류 `c90cda5`(PR #25)가 먼저 옮겼다.
- **`GetCaretFormat`의 크기 보고** — ✅ 상류 PR #26(`DrawnRunSize` 판). 상류는 제목 적용 시 run 크기를 본문 기본값으로
  초기화하므로 이 경우가 오히려 더 흔했다.
- **Word 방식 토글** — ✅ 상류 `c90cda5`(PR #25)가 먼저 옮겼다(상류 자체 구현).
- ⚠️ 그 `c90cda5`는 **제목 적용 시 run 크기를 본문 기본값으로 초기화**하기로 했다 — 포트는 **직접 지정한 크기를 유지**
  (사용자 결정). 두 리포가 여기서 갈라져 있다.

#### 하네스 함정 (실제로 밟았다)
- **첫 실행 24/24 실패는 전부 하네스였다** — 한 단계에 언두 그룹 둘(URL 입력 뒤 공백), 그리고 undo/redo
  단계에 또 Undo. 오라클이 틀리면 결함과 똑같이 보인다.
- **`UiThread.Run`은 본문당 60초다.** 전 시드를 한 본문에서 돌리면 470시드쯤부터 `TimeoutException`이
  난다(제품과 무관). 시드마다 따로 마샬링한다.
- **`Shape` 덤프는 `LogicalCells`만 그린다.** 캐럿이 피복 칸에 있을 때 덤프는 "명령이 일어나지 않은
  표"처럼 보였다(행 삽입 뒤인데 3행). **검사 지점 이름표**(`[after the step]` / `[after the inverse]`)와
  **칸 진단**(`covered=`, `table in document=`)을 넣고서야 "피복 칸"과 "분리된 표"가 갈렸다.
- 병합 드래그 끝점을 `Cells[][]`에서 뽑으면 피복 칸이 끝점이 된다(사용자는 불가능 — 2000시드 4건 중 1건이
  이것). 앵커로 매핑하되 **`rng` 소비 횟수를 그대로** 둬야 실패 시드가 계속 재현된다.

#### 동작 변경 — 문자 토글을 Word 방식으로 (사용자 결정, 2026-09-12) — 실기 확인 완료
`ToggleBold`/`Italic`/`Underline`/`Strikethrough`는 **런마다 뒤집었다** — "보통 **굵게** 보통"에 Ctrl+B는
반전을 낳았다(상류도 같다). 감사 중 발견해 판단을 물었고 Word 방식으로 결정됐다: **대상 글자가 전부
이미 그 서식이면 끄고, 아니면 모두 켠다.**
- 대상 결정은 `ResolveStyleTarget` 하나(셀 사각형 / 선택 / 캐럿 단어 / 대기)를 명령과 판정이 공유한다 —
  판정과 적용이 다른 텍스트를 볼 수 없다. 판정은 **문서를 건드리지 않고** 읽는다(`GetRichRuns`의 복제본.
  `ApplyPropertyValue`로 판정하면 체크포인트 전에 런이 쪼개진다).
- 대상 글자가 없는 대기 서식은 캐럿 서식(`CaretFormatRun` — 툴바가 보는 바로 그 런)이 결정한다. 두 번
  누르면 취소된다.
- 테스트 13: 4 토글 × 섞인 선택 · 4 × **서식으로 시작하는** 선택 · 반만 굵은 캐럿 단어 · 굵은 셀이 섞인 사각형 ·
  문단을 넘는 전부 굵음 → 끔 · 빈 셀이 섞인 전부 굵음 → 끔 · 대기 서식 가드.
  > **반증 4종, 전부 의도한 테스트만 죽었다**: 런별 뒤집기 10개 · 첫 글자가 결정 4개 · `"\n"` 문단 구분자를
  > 글자로 셈 1개 · 빈 셀 런을 셈 1개. ⚠ **처음 쓴 테스트는 "첫 글자가 결정" 규칙과 구분하지 못했다** —
  > 전부 서식 없는 글자로 시작했기 때문이다. 구분자·빈 셀 필터도 무방비였다. 셋 다 반증을 설계하다가
  > 보였고, 테스트를 더한 뒤에 교란이 잡혔다.

#### 다음 (같은 기준 — 공개 멤버 참조 0)
- **기능 플래그 게이트**(`Allow*` 7종, 약 63곳 분기, 참조 0) — 상류 `FeatureFlagTests` 8 이식 +
  `AllowLocalFileImages`(양쪽 0). `AllowRemoteImagesOnPaste`·`AllowLocalFileImages`는 개인정보 게이트다.
- **문서 API 래퍼**(`LoadJson`·`LoadHtml`·`InsertHtml`·패키지) — "손상 입력이 열린 문서를 지킨다"가
  `LoadRtf`에만 검증돼 있다.
- 하이퍼링크(`AutoLinkOnType`의 경계 규칙).
- **캐럿 크기가 제목 안의 명시 크기 런을 무시할 가능성** — `RunFontSizeAt`은 제목이면 무조건 제목 크기를
  돌려주지만, 렌더(`DrawnRunSize`)는 명시 크기를 존중한다(제목 안 36pt 런 → 20pt 캐럿?). **미측정.**
  캐럿 계열은 수정 하나가 결함 셋을 파생시킨 전력이 있어 이번에 함께 건드리지 않았다 — 프로브·반증과 따로.

### 감사: 줌 축 (2026-09-08) — 완료, 테스트 397 → 413
빌드 0/0. 상세는 CHANGELOG.

**상류와 줌 아키텍처를 대조하다 나왔다.** 어느 쪽이 나은가를 따지려고 양쪽 구현을 읽었더니 답은
"각자 자기 백엔드가 강제한 것"이었고(위 결정 사항), 진짜 문제는 따로 있었다 — **포트의 줌 수학은
테스트가 0이었다.** `Zoom`을 언급하는 테스트 파일조차 없었다. 언두/찾기와 **같은 방법으로 고른 축**이다.

- [x] `ControlZoomTests` 16개 — 좌표 왕복(연속·페이지 모드 × 배율 4종) · **그린 자리를 클릭하면 그
      글자에 떨어지는가**(포인터 경로 전체: view → `ViewToDoc` → `GetPositionFromPoint`) · 캐럿의 문서
      좌표는 배율에 불변이고 캔버스 좌표만 2배가 되는가 · `SetZoom` 클램프 · fit-to-width가 **모드**이고
      명시 줌이 그것을 빠져나가는가.
- [x] **반증 확인**: `ViewToDoc`의 `/z` 제거 + `SetZoom`의 클램프·fit 해제 제거 → **10개 실패**,
      그리고 **배율 1.0 케이스만 통과**한다(1.0에서는 누락된 나눗셈이 무연산이다) — 테스트가 줌 인수
      자체를 판별한다는 증거다.
> ⚠ `ControlCaretTests`의 100/200/300%는 **줄간격**이지 줌이 아니다. 줌 배율 커버리지는 이번이 처음이다.

### 감사: 언두/찾기 축 (2026-09-06) — 완료, 테스트 347 → 391
빌드 0/0, 퍼즈 4000시드 클린. 상세는 CHANGELOG.

**감사 대상을 정독이 아니라 grep으로 골랐다.** 이 리포에서 "어디를 볼까"의 답은 코드 냄새가 아니라
**관찰이 0인 축**이고, 그건 기계적으로 찾을 수 있다: `tests/` 전체에서 `Undo()`·`Redo()`·`FindNext`·
`FindPrev`·`ReplaceAll`·`ReplaceNext`·`GetFindMatchPosition` **호출 0건**, 퍼즈에 `PushState`/`Undo`
**0건**이었다. 전부 공개 API고, 언두는 스택 둘 + 바이트 예산 + 문서 순서 번호로 복원하는 상태 기계다.

- [x] `UndoHistoryTests` 16(헤드리스) · `ControlUndoTests` 10 · `ControlFindReplaceTests` 17.
- [x] **퍼즈에 이력 축** — 랜덤 편집열을 되감아 매 단계의 이전 형태를 복원하는지 본다. 언두가 복원하는
      것은 `Clone`이므로 **Clone 충실도 축이기도 하다**(`Background` 한 줄을 빼자 400시드 중 81건).
- [x] **반증 6회, 전부 의도한 테스트만 죽었다** — wrap 건너뛰기(감사 2.11이 기각한 그 "최적화") 2개 ·
      앵커 `_selEnd`→`_selStart` 1개 · 타이핑 합치기 제거 2개 · `UndoManager` 얕은 걷기 5개 ·
      `ReplaceAll` 체크포인트를 루프 안으로 1개 · `Paragraph.Clone` 필드 누락 81건.

> ⚠️ **공허한 테스트를 하나 썼고 반증이 잡았다.** 바이트 예산 판의 첫 버전은 스냅샷을 **4회** 푸시했는데,
> `Trim`이 스택 ≤ `MinSteps`에서 조기 반환하므로 예산 루프의 바닥 조건을 **지워도 답이 같았다**.
> 6회여야 루프가 유일한 근거가 된다. **"가드가 공허한가"를 반드시 되돌려 확인할 것**(R1·R2와 같은 계열).

- [x] **문단 서식 목록의 두 번째 사본 제거** — `Paragraph.Clone`이 `CopyFormatFrom`의 필드 13개를 손으로
      복제하고 있었다(목록이 글자 단위로 동일). 이제 `Clone`은 그걸 부른다. 동작·표면 불변.
      실패 모양이 무게다: `Clone` 쪽을 잊은 새 속성은 **편집 중엔 멀쩡하고 첫 언두에서 사라진다.**
      > **합친 뒤 반증했다.** 합친 목록에서 `Background`를 지우자 **391개 중 실패는 새 퍼즈 축 하나뿐**
      > — 즉 **오늘 이전에는 이 목록 전체가 무방비였다**(포매터 왕복은 `Clone`을 지나지 않는다).
      > 통합이 지금 안전한 이유가 그 축이 먼저 생겼다는 것이고, 반증 없이 합쳤다면 "초록이니 됐다"로
      > 끝났을 것이다. **상류도 글자 그대로 동일 → 백포트 후보.**

#### 다음 (같은 기준으로 고른 것)
- **포인터/키 배선**(`RichEditor.Input.cs` 2246줄) — "합성 불가"라는 전제의 절반은 이미 반증됐다:
  `tools/uia-probe/drive-demo.ps1`이 실입력 `SendInput`으로 데모를 타이핑시킨다. 없는 것은
  **`MOUSEINPUT`과 상태 되읽기**뿐이고, 되읽기는 같은 프로브가 이미 `TextPattern`으로 하고 있다.
- **상류 대조 재실행** — 마지막 수렴 이후 상류에 커밋이 쌓였다(`a66b472` 툴바 공유 브러시 스레드 친화,
  `ad08d51` hot path 할당, `d1d86a8` 감사 반영). `33ee7b5`(이미지 핸들을 선택 시에만)는 **대조 완료·해당
  없음** — 포트가 이미 `_selectedBlock` 동일성으로 게이트한다(상류가 이쪽으로 수렴한 건).

### 컨트롤 레벨 자동 검증 (2026-08-06) — 1단계 완료
빌드 0/0, 테스트 **160 → 181**. 상세는 CHANGELOG.

**핵심 사실**: 테스트 프로젝트는 이미 `UseWinUI` + `WindowsAppSDKSelfContained`라 Windows App SDK
바이너리가 테스트 어셈블리 옆에 있고 **부트스트래퍼가 필요 없다**. 없던 것은 **디스패처가 달린 스레드**
하나뿐이었다. 전용 STA 스레드에서 `Application.Start`를 부르면 `RichEditor`·`RichEditorToolbar`·
`RichEditorView`가 전부 생성되고, 편집 명령·문서 API·트리 워크가 다 된다.

- [x] **`UiThread`** — 런타임을 프로세스당 한 번 띄우고 테스트 본문을 마샬링(예외는 원래 스택으로 재던짐).
- [x] **`ControlLevelTests` 13개** — 그동안 사람만 확인하던 것들을 고정: 손상 RTF가 열린 문서를 지킴 ·
      **툴바 버튼 전부가 밀도 3종에서 포커스 거부**(트랙 C의 회귀 가드) · `GetPlainText` 셀 재귀 ·
      `RichEditorView` 구성/찾기 바 · 실제 `Window` 호스팅 후 `FocusEditor()` 성공.
- [x] **첫 사용에서 결함 1건 적발** — 잘린 RTF가 열린 문서를 조용히 덮어썼다(아래).

> ⚠️ **비주얼 트리 밖 `Measure`/`Arrange`는 스택 오버플로로 프로세스를 죽인다**(0xC00000FD).
> 레이아웃이 필요한 테스트는 실제 `Window`에 넣을 것.
> ⚠️ Application은 **프로세스당 하나**라 전 테스트가 공유한다. static 상태를 바꾸면 되돌릴 것.

#### 2단계 — 캐럿 기하와 세로 이동 (완료, 테스트 181 → 198)
결함 이력이 가장 나쁜 두 영역이다. `ControlCaretTests` 17개가 100/200/300%를 모두 돈다.
캐럿 높이 = 글자 높이 · 커스텀 간격의 baseline 식(`LineTop + 줄상자×0.8 − 높이×0.8`) · 빈 줄 동일 규칙 ·
세로 이동이 세 배율에서 문단을 넘음 · 줄바꿈 문단 안에서 문단 이탈 안 함 · **셀 안 줄 사이 이동**.

> ⚠ **자연 간격에는 위 baseline 식이 적용되지 않는다**(폰트 자체 baseline을 쓴다, 약 0.3px 차이).
> 0.8 비율은 **커스텀(Uniform) 간격에서 DirectWrite에 지정하는 값**이고 역산도 같은 상수를 쓴다.

> **반증에 세 번 실패했다.** probe를 당시의 캐럿 기준으로 되돌려도 ① 최상위 문단 사이 ② 셀 안 두 문단
> 사이는 통과한다 — `AdjacentTopLevelParagraph` 폴백과 문단 경계 넘기가 결함을 가린다. **셀 안 줄바꿈된
> 한 문단의 줄 사이**에만 폴백이 없고 거기서 300%에서 캐럿이 전혀 안 움직인다(100%는 정상).
> **회귀 테스트는 고친 코드를 되돌려 실패시켜 보기 전까지 그 결함을 잡는다고 말할 수 없다.**

#### 3단계 — 클립보드 경로 (완료, 테스트 198 → 212)
Word·HWP 상호운용이 전적으로 여기 달렸는데 검증이 없었다. `ControlClipboardTests` 14개가 **실제 시스템
클립보드**로 돈다(가짜를 쓰면 가짜를 시험하게 된다 — Windows의 LF→CRLF 정규화에 내부 스냅샷 가드가 의존).
세 플레이버 존재 · CRLF · 외부/내부 붙여넣기 경로 · Ctrl+Shift+V · 이미지 복사가 낡은 스냅샷 제거 ·
`LooksTabular`.
> ⚠ **이 스위트는 실제 클립보드를 덮어쓴다.** `ClipboardGuard`가 텍스트는 복원하지만 더 풍부한 내용은
> 잃는다(임의 `DataPackage`를 스냅샷할 수단이 Windows에 없다).

**하네스 결함 3건을 함께 고쳤고 셋 다 처음엔 제품 결함처럼 보였다.**
- `SynchronizationContext` 미설치 → `await` 후 continuation이 스레드풀로 가고 DP 읽기가 `COMException`.
  동기 완료 시엔 안 나서 **간헐적**이었다.
- **UI 테스트 클래스 병렬 실행** → UI 스레드·호스트 창이 하나뿐이라 서로를 밀어냈다. 캐럿 17개가
  결정적으로 실패했는데 단독 실행은 전부 통과. `UiTests.Collection`으로 직렬화 → 전체 **1분 → 7초**.
- 창 `Content` 교체 → 안정적인 루트 패널의 자식 교체.
> **교훈**: 하네스가 미숙하면 증상이 정확히 제품 결함처럼 보인다. "붙여넣기가 조용히 아무것도 안 한다"는
> 테스트가 `Document.Blocks.Add()`로 문서를 만들어 `Parent`가 비었던 탓이었다(제품은 `Document` 대입으로
> 배선한다). **문서는 제품이 만드는 방식대로 만들 것.**

#### 4단계 — 컨텍스트 메뉴와 IME (완료, 테스트 212 → 223)
둘 다 **끝까지 구동할 수 없다**(게이트가 합성 불가능한 WinRT 이벤트 args 뒤에 있다). 도달 가능한 것은
메뉴 빌더와 IME가 시스템에 노출하는 상태이고, 사용자에게 도달하는 실패는 거기 산다.
메뉴: 클립보드 동사가 선택을 따르는가 · `ShowFormattingMenu`가 그룹을 더하는가 · 링크 동사 배타성 ·
**모든 항목에 보이는 라벨이 있는가**(현지화 키 누락 = 빈 라벨). IME: **인라인 오브젝트 = U+FFFC 한 글자** ·
버퍼가 캐럿 문단을 따라감 · 같은 길이 편집 보고 · 포커스 상실 시 조합 종료 · 선택 범위 일치.

> **반증에서 또 걸렀다**: 같은 길이 편집 테스트의 첫 판은 `_imeBufferText`를 봤는데 그 필드는 **분기와
> 무관하게 매번 대입**된다. 길이만 비교하도록 되돌려도 통과했다. 두 분기를 바깥에서 구분하는 신호는
> 보고 분기만 초기화하는 `_imeRangeDelta`뿐이었다.
> **클립보드 경쟁**: `SetContent` 직후 읽기가 이전 내용을 볼 수 있다. "텍스트가 있는가"로는 부족하고
> (이전 테스트가 남긴 것에 즉시 만족한다) **방금 복사한 텍스트**를 기다려야 한다. 10회 연속 그린.

#### 5단계 — 페이지네이션 (완료, 테스트 223 → 241)
`ComputePageBreaks`가 페이지 상자를 매개변수로 받아 임의 크기로 돌릴 수 있다. 용지 치수 6종 · 가로 방향 ·
연속의 A4 폴백 · **페이지 시작이 엄격히 증가**(빈 페이지도 무한 루프도 없음) · **어느 페이지도 상자보다
크지 않음** · 쪽수가 내용/페이지 크기에 양방향으로 반응 · 재계산 안정성 · 빈 문서 1쪽.

> ⚠ **덮지 못한 것**: `PlaceAtom`의 `y > pageStart` 전진 가드는 이 18개가 **판별하지 못한다.** 가드를 빼고
> 페이지보다 큰 첫 문단·이미지·큰 표·**1픽셀 페이지**까지 시도했지만 넷 다 그린이었다 — 첫 원자가 놓일 때
> `y`는 이미 페이지 시작을 지나 있다. 쪽수로 세는 판은 **정상 코드에서도 실패**했다(정규화가 넣는 앞뒤
> 문단 때문에 페이지 시작 셋이 정상). 다음에 이 가드를 건드린다면 이 사실을 먼저 볼 것.

> **클립보드 플레이키**: Windows 클립보드는 **다른 프로세스와 공유되는 잠금 자원**이라(기록·동기화 기본
> 켜짐) 어떤 호출이든 실패하거나 경쟁에서 질 수 있다. 복사 전체와 모든 읽기를 재시도한다. 20회 중 19회
> 클린 — 완전한 결정성은 살아 있는 데스크톱에서 보장되지 않는다.

#### 6단계 — 이벤트 args 뒤의 게이트를 순수 함수로 (완료, 테스트 241 → 265)
외부 리뷰가 제안하고 이 로드맵이 "테스트 인프라에 흡수"로 미뤄 둔 추출. **결정과 실행을 갈랐다** —
핸들러는 히트테스트와 상태 변경을 계속 맡고, 우선순위 순서와 오프셋 산술만 나온다.
- `ChooseContextMenu` — 객체 > 읽기 전용 > (선택 없을 때만) 링크 > 전체.
- `ChoosePointerTarget` — **모든 리사이즈 핸들이 모든 선택 클릭보다 먼저**(그랩 밴드가 rect 밖으로 나가
  이웃이 삼킨 전력이 있다), 그다음 안쪽 우선.
- `MapImeRange`/`MapImeCaret` — 문단 안으로 클램프, **끝이 시작보다 앞서지 않음**.

`ExtractedGateTests` 24개는 **런타임 없이 0.1초**에 돈다. **반증 확인**: 우선순위를 뒤집으면 4개가 실패한다.
공개 표면 불변(전부 `internal`).
> ⚠ xunit은 테스트 클래스가 public이어야 해서 **internal enum을 테스트 시그니처에 쓸 수 없다**(본문에서만).
> ⚠ 소스를 PowerShell `Set-Content`로 고치지 말 것 — 인코딩 지정 없이 쓰면 **비ASCII가 깨진다**(이번에
> 실제로 깨뜨렸고 백업에서 복원했다). 그리고 `Copy-Item` 복원은 **타임스탬프까지 되돌려** MSBuild 증분
> 빌드가 건너뛴다 — 복원 후 `LastWriteTime`을 갱신할 것.

**다음 단계**: **포인터/키 주입은 여전히 없다**(합성 불가). 남은 것은 핸들러 자체의 배선(히트테스트 결과가
실제로 맞는가, 상태 변경이 실제로 일어나는가)이고, 그건 주입 없이는 닿지 않는다. 실기 확인이 계속 필요한
영역이 바로 거기다.

#### 잘린 RTF가 열린 문서를 덮어쓰던 것 (하네스 첫 사용에서 발견)
`TryParse`의 XML 문서는 "중간에 중단된 파싱은 실패를 보고한다"고 약속했지만 **사실이 아니었다.** 잘림은
예외를 던지지 않는다 — 리더가 입력을 다 쓰고 읽은 것을 마무리하므로 **더 짧은 문서를 깨끗이 읽은 것과
구별되지 않았다.** 검출하던 것은 숫자 오버플로처럼 **던지는** 입력뿐이었고, 상류의 damaged 픽스처도
그 한 종류다.
- RTF는 중괄호 균형 형식이므로 **끝났을 때 열린 채 남은 그룹**이 곧 잘림이다(`UnclosedGroups`).
- **`Parse`는 관대함을 유지**한다(붙여넣기 경로 — 잘린 조각도 읽을 만큼은 살린다). 엄격함은 `TryParse`
  몫이라는 것이 두 진입점을 나눈 원래 이유다.
> **상류에도 같은 구멍이 있다** — 백포트 후보.

✅ **2026-08-05 리뷰 대응분은 실기 확인까지 끝났다**: 손상 RTF가 문서를 지우지 않는 것(`2cef3de`),
캐럿 깜빡임이 OS 설정을 따르고 끄면 정지하되 보이는 것(`11379da`), 툴바 Import/Export가 정상 경로에서
그대로 동작하는 것(`fd4d849`) 모두 확인. 깜빡임은 `tools/measure-caret-blink.ps1`로 **측정**했다 —
항목 1(컨트롤 레벨 자동 검증)의 첫 조각이다. 툴바 `FontSizes`/`Palette` 세터 가드도
검증된다 — static 브러시 6개를 지연 생성으로 바꿔 헤드리스로 만질 수 있게 했다(덤으로
`Application.Start` 이전 할당도 열렸다). 테스트 118 → 131.

#### 상류(AvaloniaRichEditor) 백포트 ✅ 완료 (2026-08-05, `fd850ad` + `95e2d36`)
아래 3건 전부 상류에 이식했다. 이름은 **양쪽 동일**하게 맞춰 표면 수렴을 유지했다
(`RtfDocumentFormatter.TryParse`, `RichEditorDiagnostics`/`RichEditorFaultEventArgs`).
상류 테스트 **618 → 631**, 경고 0.
- 상류에서는 **컨트롤 계층 테스트가 가능**해서(`[AvaloniaFact]` + headless) 이쪽에서 못 한 검증을 붙였다:
  `LoadRtf_DamagedInput_KeepsTheOpenDocument`가 문서 인스턴스가 **교체조차 되지 않음**을 단정한다.
  같은 검증이 WinUI 쪽에는 여전히 없다 — 1.1 항목 1이 필요한 이유가 여기서도 확인된다.
- 삼킴 지점은 상류도 **38곳 전부** 배선했다(부분 배선은 "구독했는데 안 보이니 문제없음" 함정).
- ⚠️ 상류 작업 트리에 사용자 README 작업분이 있었다 — 스테이징에서 제외했다.

<details><summary>대조 근거 (당시 기록)</summary>


이번 수정 3건은 **포팅이 만든 결함이 아니라 양쪽이 공유하는 결함**이다. 상류 코드를 직접 대조해 확인했다.

1. **손상 RTF가 문서를 지운다** — 상류 `LoadRtf`는 수정 전과 **글자 그대로 동일**하고
   `RtfDocumentFormatter.Parse`도 `catch { return new FlowDocument(); }` 그대로다. 결정적으로 상류
   `LoadJson`에는 *"손상된 파일은 빈 문서로 읽지 않고 보고한다 — 안 그러면 빈 페이지가 뜨고 저장이
   원본을 덮는다"* 는 주석이 **이미 있다**. 양쪽 다 그 계약을 세우고 양쪽 다 RTF만 빠뜨렸다 →
   설계가 아니라 공통 누락. (우리 쪽 해법: `TryParse` + `LoadRtf`가 실패 시 문서 유지, `2cef3de`.)
2. **툴바 파일 액션의 fire-and-forget 구멍** — 상류도 `() => _ = ImportAsync()`이고
   `OpenFilePickerAsync`가 `try` **밖**(`PageFile.cs:290` vs try `:296`), `ExportAsync`는 try/catch가
   **아예 없다**. 피커가 던지면 관측되지 않은 Task로 소멸하는 것까지 동일. (`fd4d849`.)
3. (판단 필요) **진단 훅** — 상류에는 `RichEditorDiagnostics` 상당물이 없다. 1·2와 달리 공개 표면이
   커지는 결정이라 그쪽에서 별도 판단할 일. 다만 2번을 추적 가능하게 만든 것이 이 훅이었다.

표면 수렴 상태: `TryParse`, `RichEditorDiagnostics`, `RichEditorToolbar.FontSizes`/`Palette` **전부 양쪽
동일**하다. 기본값은 각자 것을 유지했다(상류 폰트 크기 집합이 원래 다르다 — 36 있고 32·40·60 없음).

</details>

#### 상류 포팅이 되돌려준 것 (2026-08-05, `7190e15`)
툴바 커스터마이즈를 상류에 이식하면서 **양쪽에 대한 사실이 두 개 드러났다**. 눈으로 옮겼으면 놓쳤을
것들이고, 둘 다 테스트가 잡았다.
- **`Palette` 오타 내성은 우연히 얻은 것이었다.** WinUI의 `ParseHex`는 실패 시 검정으로 대체하지만,
  상류 `Color.Parse`는 **던진다** — 팔레트가 private 상수일 땐 무해했지만 호스트가 넣는 순간 오타 하나가
  툴바 빌드를 통째로 죽인다. 상류를 WinUI 쪽 동작에 맞췄다. **우리가 이 속성을 문서화할 때 "던지지
  않는다"고 쓸 수 있었던 건 설계가 아니라 `ParseHex`의 부수 효과였다**는 뜻이다.
- **상류엔 잠복 결함이 있었다** — 크기 콤보가 항목 텍스트로 선택을 매칭하는데 항목은 int로 쓰고 캐럿
  반영은 `(int)` 캐스트라, 10.5pt 같은 값은 영영 선택 표시가 안 됐다. 같은 포매터로 통일해 고쳤다.
  WinUI는 `PtText()`로 이미 한 곳을 통과시키고 있어 해당 없음.

**가드 검증**: 양쪽 다 헤드리스로 된다. 상류는 원래 가능했고(631 → 644), WinUI는 static 브러시 6개를
지연 생성으로 바꿔 열었다(118 → 131). 다만 상류가 한 걸음 더 간다 — **"커스텀 배열이 실제로 콤보에
반영되는가"** 까지 단정한다. WinUI는 툴바 인스턴스 생성 자체가 런타임을 요구해 여전히 세터까지만이다.


#### 외부 코드 리뷰 분류 (2026-08-05)
받은 리뷰 5개 항목을 코드와 대조했다. **사실 관계는 대체로 맞았고 진단은 절반이 빗나갔다** —
리뷰가 `tests/`·퍼즈·상류 `AvaloniaRichEditor`·1.0 API 동결을 하나도 언급하지 않았는데, 이 코드베이스를
어떻게 평가할지는 정확히 그 넷이 정한다("단위 테스트가 거의 불가능"은 테스트 110개로 반증).

- **수용·완료 ①**: RTF 파싱 실패가 관측 불가 → CHANGELOG `[Unreleased]`. 리뷰에서 **유일하게
  데이터 손실로 이어지던** 항목이다.
- **수용·완료 ②**: 캐럿 깜빡임·더블클릭 간격의 하드코딩 → `SystemInputSettings`로 OS에서 읽는다.
  리뷰는 이걸 "매직 넘버" 카테고리로 묶었지만 실제 무게는 **접근성**이다(깜빡임 끄기 설정 무시).
- **수용·완료 ③**: 삼킨 예외가 관측 불가 → `RichEditorDiagnostics.Fault`. 리뷰의 "예외 묵살은 최악의
  안티패턴" 프레임은 기각했다(대부분 주석 달린 의도적 네이티브 폴백이고, 동작은 옳다). 유효한 것은
  **관측 수단 부재**뿐이었고 그것만 고쳤다 — 폴백 동작은 한 곳도 바꾸지 않았다.
- **수용·대기**: 히트테스트/세로이동을 순수 함수로 분리(**단, 1.1 항목 1의
  테스트 인프라에 흡수** — "God Object 해체"가 목적이 아니다. 캐럿 계열은 수정 하나가 결함 셋을
  파생시킨 전력이 있어 단독 리팩토링으로 먼저 하지 않는다).
- **기각**: ① XAML 도입 — 제약이 곧 대안이다(`x:Bind`가 AOT 함정이고, 라이브러리가 XAML을 쓰면 `.pri`가
  필요해져 code-only로 검증된 NuGet 소비 경로가 깨진다). ② 히트테스트를 레이아웃에서 분리 — **비자명
  규칙 #1(단일 TextLayout이 진실의 원천)의 정반대**다. 분리하면 레이아웃이 둘이 되고 그게 클릭 위치와
  글자가 어긋나는 계열 버그의 원인이다. 게다가 "성능 최적화가 어렵다"고 지목된
  `GetPositionFromPoint`는 이미 이진 검색 O(log blocks)에 레이아웃 지연 실체화가 들어가 있다.

### 전수조사 (2026-08-06) — 완료, 결함 4건
빌드 0/0, 테스트 **142/142**(131 → 142), 퍼즈 CI 기본값 **24 → 400시드**. 상세는 CHANGELOG.

**한 문장 요약: 시드 24가 이 퍼즈가 볼 수 있는 것의 상한이었다.** 이번에 찾은 4건은 전부 시드 24 밖에
있었고(53 · 1559 · 482 · 26계열), 그래서 전수조사 7회와 CI를 통과해 왔다. 400시드는 3초다.

- [x] **중첩 글머리표가 HTML 왕복에서 순서 역전** — `ParseList`가 하위 리스트와 `<li>`를 두 번 나눠
      순회해 문서 순서를 버렸다. **이 두 번 순회 자체가 다른 결함의 수정이 넣은 것**이다(로드맵의
      "들여쓴 목록만 있는 문서가 마크업 텍스트로 변질" 수정). 한 번 순회로 합치고 두 형태를 각각 고정.
- [x] **`MergeCells`가 고아 셀을 만든다** — 기존 병합을 가로지르면 앵커를 덮어쓰면서 그 앵커가 소유하던
      셀을 해제하지 않아, 피복 표시만 남은 셀이 `LogicalCells()`에서 영영 사라졌다(표에 논리 셀이 하나도
      안 남는 상태까지 갔다). 범위를 **닿는 병합 전체로 확장 → 해제 → 재스탬프**.
      > **핵심 계약**: 병합 격자를 건드리는 연산은 피복 표시를 **반드시 짝으로** 남긴다 — 모든 피복 셀은
      > 자기를 실제로 덮는 앵커를 가져야 한다. span을 줄이거나 앵커를 덮어쓰는 코드는 그 차집합을 해제할 것.
- [x] **빈 문서를 HTML로 저장→열기가 에디터 태그를 본문으로** — 원문 폴백이 "마크업이 아니었다"용인데
      내용 없는 마크업에도 발동했다. 요소 노드 유무로 분기.
- [x] **병합 셀이 2회째 HTML 왕복에서 단어 경계 상실**(1.0부터 알려진 미수정) — 인라인 형제 사이의 공백은
      구분자, 닫는 태그 앞은 여백. **미뤘다가 뒤에 인라인이 올 때만** 넣는다(즉시 넣는 판은 문단 끝에
      공백을 쌓아 실패를 4 → 12건으로 늘렸다).
- [x] **퍼즈 자체 보강** — 불변식이 피복 셀의 앵커를 검증하지 않고 `continue`하던 구멍을 메꿨다(메꾸자마자
      60시드에서 9건, 전부 `MergeCells`로 수렴). 왕복 테스트는 첫 실패에서 멈추지 않고 모아서 보고한다.
      시드는 `RICHEDITOR_FUZZ_SEEDS`로 넓힌다.

#### 상류 백포트 후보 (전부 대조로 확인 — 상류에 그대로 있음)
- **`MergeCells` 고아 셀**: 상류 `TableBlock.cs` 세 줄(`ColSpans[r0][c0]=` / `RowSpans[r0][c0]=` /
  `StampCovered`)이 **글자 그대로 동일**하고 해제 단계가 없다.
- **빈 문서 원문 폴백**: 상류 `BuildDocument`도 동일. 상류는 **셀** 경로만 올바르다(`cell.Blocks.Count == 0`
  → 빈 문단). 문서 경로에 같은 처리를 넣으면 된다.
- **들여쓴 항목만 있는 리스트가 사라지는 것**: 상류엔 그 수정 **자체가 없다** — 즉 원래 결함이 남아 있다.
  백포트할 때 두 번 순회가 아니라 **이번의 한 번 순회 판**을 옮길 것(두 번 순회는 순서 역전을 낳는다).
- **하이퍼링크의 명시 색이 링크 파랑으로 덮이는 것**은 상류와 주석까지 동일한 **의도적 트레이드오프**라
  백포트 대상이 아니다(아래 "알려진 한계" 참조).

### HTML 왕복 손실 3건 (2026-08-06) — 완료
전수조사에서 "나가는 HTML이 바뀌어 보류"로 뒀던 것들을 이어서 처리했다. 셋 다 **자체 문서만 복원하고
외부 HTML 동작은 유지**하는 방향(인라인 표에 이미 쓰던 `data-are-*` 마커 idiom). 퍼즈 **20000시드 통과**.

- [x] **연속 공백이 접힘** — 공백 뒤 공백을 `&nbsp;`로(Word 방식), 런의 첫 공백은 일반 공백으로 남겨
      줄바꿈 지점을 보존. reader의 `\s`가 **U+00A0도 매치**해서 살아남으라고 넣은 문자를 접고 있었다.
      > **핵심 계약**: HTML 공백 판정에 `\s`나 `IsNullOrWhiteSpace`를 쓰지 말 것 — 둘 다 nbsp를
      > 공백으로 센다. 접히는 공백은 ASCII 5종뿐이고 **nbsp는 내용**이다.
      > **버려지는 자리 3종만** 인코딩한다: 문단 끝 · 소프트 개행 앞 · **공백만으로 된 Run**.
      > 셋째가 핵심이다 — 공백-only Run은 정의상 저작된 내용인데 나가고 나면 들여쓰기와 구분이 안 돼
      > 파서의 구분자 로직이 버렸다. `MergeCells`의 결합 구분자가 정확히 이 모양이다.
      > ⚠️ "여는 Run의 앞 공백 전부"로 넓히면 3000시드 중 **71건에서 2개로 늘어난다**(실측 후 되돌림).
      > 조건은 "공백으로 시작"이 아니라 **"전부 공백"**이어야 한다.
- [x] **하이퍼링크의 명시 색이 링크 파랑으로 덮임** — `data-are-fg` 마커로 문서의 색과 사이트의 색을
      구분. 외부 앵커는 종전대로 파랑(양방향 테스트로 고정). 마커는 span에, 색이 칠해지는 텍스트는 그
      자식이라 파서에 플래그를 **상속**시킨다.
- [x] **목록 항목의 제목 레벨 소실** — `data-are-h`로 함께 보낸다(태그는 `li`가 이긴다).

### 퍼즈 커버리지 구멍 — 이미지·구분선·빈 문단 (2026-08-06) — 완료, 결함 5건
**퍼즈가 `ImageBlock`·`InlineImage`·`DividerBlock`·빈 문단을 한 번도 만들지 않았다.** `Shape()`는 넷 다
출력할 줄 아는데 `ApplyRandomOp`가 생성하지 않아 축 전체가 미검증이었다. 손으로 28조합(4포맷 × 7형태)을
찌른 뒤 **퍼즈에 축을 추가**했고, 추가하자마자 400시드에서 16건이 더 나왔다. 5000시드 클린, 테스트 160.

- [x] RTF 블록 이미지 밑에 **빈 문단이 왕복마다 증식**(`\pard <pict>\par`의 `\par`는 구조).
- [x] RTF **표 뒤 이미지가 표 앞으로 이동**(`FinalizeTable` 전에 추가돼 순서 역전).
- [x] RTF **구분선이 빈 문단이 됨** — 쓰는 쪽만 있고 `\brdrb`를 **읽는 쪽이 없었다**.
- [x] HTML **이미지 하나만 든 문단이 앞 문단에 흡수** — 인라인 표와 같은 `data-are-opens`로 해결.
- [x] HTML **빈 문단 소실** — `data-are-empty` 마커(외부 HTML의 빈 요소는 계속 버려야 한다).

> **교훈**: `Shape()`가 출력할 줄 아는 형태를 `ApplyRandomOp`가 만들지 않으면 그 축은 **조용히 0% 커버**다.
> 퍼즈에 모델 형태를 추가할 때는 생성과 관찰을 **짝으로** 볼 것.

### 외부 감사 보고서 전수 검증 (2026-08-26) — 완료, 수정 13건
외부 도구가 만든 31개 항목 감사 보고서(2026-08-17)를 코드와 1:1 대조했다.
**확인 11 / 반증 4 / 기각 7 / 부분사실·미수정 3 / 성능 보류 6**, 그리고 검증 중 **보고서가 놓친 2건**을
더 찾아 코드 수정은 13건. 빌드 0/0, 테스트 **324 → 345**, 퍼즈 2000시드 클린.

> **원본 보고서는 리포에 두지 않기로 했다.** 항목별 판정을 달아 `docs/`에 남겼다가 지웠다 —
> 남겨 두는 것 자체가 위험하기 때문이다: 31개 중 신뢰할 수 있는 것이 11개인데 원문은 전부 확정처럼
> 쓰여 있고 Before/After "수정 코드"까지 붙어 있어, 배너를 지나쳐 한 항목을 집어 적용하면 **회귀가 난다**
> (2.11이 정확히 그렇다). 검증에서 나온 값은 전부 여기와 CHANGELOG에 있으므로 원문은 중복이다.
> **다음에 같은 종류의 보고서를 받으면 이 절부터 볼 것** — 아래 기각 목록이 그대로 재사용된다.

**한 문장 요약: 사실 관계는 3분의 1만 맞았고, 맞은 것은 진짜였다** — 2026-08-05 외부 리뷰와 같은 양상.
결정적인 것은 **읽어서는 갈리지 않는 5건을 프로브로 돌려 본 것**이고, 그 다섯 중 셋이 보고서를 반증했다.

> **커버리지 주장은 진짜였고 진단이 틀렸다.** 보고서가 실은 파일 인벤토리는 **73개 전부, 디렉터리별
> 24/5/42/2까지 실제 트리와 완전히 일치**한다(기계적으로 대조함). "전부 읽었다"는 사실이고, 그 위에
> 얹힌 결론이 3분의 2가 틀린 것이다 — **커버리지와 진단 신뢰도는 별개로 평가할 것.**

#### 확인 → 수정 (보고서 11건 + 보고서가 놓친 2건). 상세는 CHANGELOG
표 열 리사이즈 `Math.Clamp` 크래시 · `Extract` 빈 격자 크래시 · RTF `-` 오토큰화로 문서 전멸 ·
RTF가 비트맵-only 이미지 폐기 · Backspace/Delete/화살표가 서로게이트 페어 분할 · 디바이스 재생성 때
이미지 캐시 잔존 · 셀 이미지 우클릭 미인식 · 셀 안 블록↔인라인 변환 무동작(**양방향**) · CSS 퍼센트
색상 · UIA 텍스트/선택 알림 부재 · 대화상자 중복 시 fire-and-forget으로 사라지던 예외(**2곳**).

> **읽기로 확정된 것과 프로브로 뒤집힌 것.** 다섯 항목은 정독으로 갈리지 않아 일회용 테스트를 심었다.
> **확인 3**: ① `Extract(0,0,0,0)` → **던진다** ② `"a😀"` Backspace → **`[0061 D83D]`**
> ③ `{\rtf1\ansi\fs-x hello}` → **빈 문서**(보고서보다 나쁨 — 예외가 아니라 데이터 손실이 본질)
> ④ `rgb(100%,0%,0%)` → **null**.
> **반증 3**: 인쇄 디바이스(1.4) · 혼합 글꼴 줄 메트릭(2.7) · 페이지 상단 갭 클릭(2.8, 캐럿은
> **문단 0 오프셋 0**으로 정상 착지).

#### 기각 — 근거와 함께 (재조사 방지)
- **1.4 "이미지 든 문서 인쇄는 100% 크래시"(P0) — 반증.** 인쇄 타깃은 `GetSharedDevice()`, `ImageCache`는
  `_canvas`로 디코드하는데 **Win2D의 XAML 컨트롤은 기본이 공유 디바이스**(`UseSharedDevice=true`)라 같은
  디바이스다. 이미지가 든 문서로 `RenderPrintPage`를 **두 번**(1회차가 디코드를 띄우고 2회차가 그린다)
  돌려 예외 없음을 실측했다.
- **2.7 "AOT 줄 메트릭 폴백이 글꼴 크기 혼합 줄을 둘로 쪼갠다"(P1) — 반증.** 전제가 틀렸다.
  DirectWrite의 `GetCharacterRegions`는 **서식 런이 아니라 줄 단위**로 돌려준다 — `"small BIG small"`
  (12pt/32pt 혼합)에서 **리전은 하나**(`[0+15 y=0.00 h=42.56]`)였고 native `LineMetrics`도 1줄,
  폴백 재구성도 1줄이었다. `LineMetricsFromRegions`의 `b.Y > lineTop + 0.5` 판정은 옳다.
- **2.8 "페이지 상단 갭 클릭이 음수 좌표를 만들어 캐럿이 튄다"(P1) — 반증.** 좌표가 음수인 것은 **사실**
  이다(A4 페이지 모드에서 view(60,0) → doc(−213, −54); 페이지가 데스크에 가운데 정렬돼 X도 음수다).
  그러나 `GetPositionFromPoint`가 그걸 **문단 0 오프셋 0**으로 돌려준다 — 첫 줄 위를 클릭하면 문서
  첫머리로 가는 것이 맞는 동작이다. 제안된 `Math.Clamp(localY, 0, PaperContentHeight)`는 무의미하다.
  > ⚠️ 이 판정은 **폴백에 속지 않도록** 여러 지점을 함께 찍어 확인했다 — 본문 안쪽(view 60,200)은
  > 문단 2로 정상 분해되므로 "무조건 문단 0" 폴백이 아니다.
- **1.6 `ImageMime.Detect` NRE** — 시그니처가 `byte[]`(nullable 아님)이고 `Nullable enable`이라
  null을 넘기는 호출자가 컴파일되지 않는다. 보고서는 존재하지 않는 `byte[]?`를 인용했다.
- **2.11 "wrap 검색이 전체를 재스캔"** — 제안된 수정(`pi < fromPi`)은 **회귀다**: wrap은 시작 문단의
  `fromOff` *앞쪽* 매치를 반드시 봐야 한다. 정확한 최적화는 `pi <= fromPi`이고 동작은 동일 — 안 한다.
- **4.7 "빈 검색어가 글자 수만큼 루프"** — `SetFindHighlight`가 빈 문자열을 **null로 정규화**하고
  `FindHighlightQuery`는 그 경로로만 설정된다. 도달 불가.
- **3.3 `[ThreadStatic]` 플래그 미해제** — `ParseHtml`과 `ParseHtmlAsync` **둘 다** 진입 시 무조건
  대입한다. 파싱 밖에서 읽는 곳도 없다. "영구 전파"는 사실이 아니다.
- **2.12 TSV 붙여넣기 `InvalidCastException`** — 캐스팅 대상은 **바로 위 줄에서 만든**
  `new TableBlock(...)`이고 `TableCell` 생성자가 `Run`이 든 문단을 넣는다. 외부 입력이 아니다.
- **2.6 `DeleteSelectedBlock` 루프 방향** — 앞으로 스캔하는 것이 의도다(`i >= 0`은 죽은 조건이지만
  무해). `NormalizeBlockList`가 문서 끝에 문단을 보장하므로 "첫 페이지로 튄다"는 결과가 안 나온다.
- **2.1 역직렬화 `Parent` 누락** — 사실이지만 **컨트롤 경로에서는 무해**하다: 문서는 `Document` 대입을
  거치고 그것이 `OnDocumentAssigned` → `UpdateParents`를 부른다(언두 복원도 동일 경로). 모델 API를
  에디터 없이 직접 쓰는 호스트에만 해당 — 로드맵이 이미 기록한 함정("문서는 제품이 만드는 방식대로
  만들 것")과 같은 것. **미수정, 알려진 경계로 남긴다.**
- **2.5 병합 셀 선택 경계 왜곡** — 산술은 맞다(피복 셀의 `SpanOf`는 `(0,0)`이라 사각형이 뒤집힌다).
  다만 **피복 셀에는 캐럿이 들어갈 수 없다**(그리기·히트테스트·내비게이션이 전부 `LogicalCells()`,
  `MergeCells`는 피복 셀 내용을 앵커로 옮긴다). 도달 경로 없이 방어 코드를 넣지 않는다.
  > ⚠ **2026-09-11 정정**: 이 전제는 **표 행/열 명령 네 개에서 거짓이었다** — 캐럿을 고정 슬롯에 두는데
  > 그 슬롯이 피복일 수 있었다(명령 퍼즈가 2000시드에서 발견). `CellCaretTarget`로 고친 뒤 참이 됐고,
  > 이제 `ControlTableCommandTests`와 명령 퍼즈의 캐럿 도달성 검사가 지킨다. 기각 결론은 유지.
- **2.10 인라인 표 안 Ctrl+A 체인 단절** — "단절"이 아니다. 마지막 단계로 **문서 전체에 도달한다**.
  인라인 표가 셀 안에 있을 때 중간 단계(바깥 블록 표)를 건너뛰는 것은 사실이나 경미.
- **3.1 인쇄 비트맵 static 누수** — 설계이고 주석에 있다(인쇄 시스템이 관리 참조를 되잡지 않으므로
  이 필드들이 job의 유일한 강한 참조다). 취소는 `PrintTask.Completed` → `Release()`로 회수되고,
  최악의 경우도 **다음 인쇄까지 한 job분**으로 유계다.
- **3.2 IME/입력 이벤트 미해제 누수** — 전부 인스턴스↔인스턴스이고, `RichEditor`는 **정적 이벤트를
  구독하지 않는다**(그렇게 하는 툴바/뷰는 `Unloaded`에서 해제한다). 메모리 하네스 실측이 "누수 없음"이다.
- **성능 5건(4.2~4.6)** — 로드맵 규칙("성능 가설은 계측으로 먼저")에 따라 **미수정**. 항목별 근거:
  - ~~`4.1`~~ → **계측 후 수정 완료**(아래 절). 규칙대로 재고, 숫자가 나온 뒤에 고쳤다.
  - `4.2` **`TextRange`의 문단 리스트 반복 생성** — 방향은 맞다(`BlockWalk`가 이미 지연 열거자를 소유).
    다만 R2에서 같은 축을 같은 세션 연속 A/B로 재 봤을 때 **1.358ms vs 1.417ms로 측정 한계 이하**였다.
  - `4.3` **HTML 파서의 동적 Regex** — `[GeneratedRegex]`는 AOT와도 맞아 방향은 옳으나, 붙여넣기는
    키 입력 경로가 아니라 사용자 주도의 드문 연산이다.
  - `4.4` **인쇄가 화면 `_layoutCache`를 날림** — 사실이나 인쇄/PDF는 드문 연산이고, 격리를 넣으면
    `WithPrintLayout`의 단순한 저장/복원 구조가 복잡해진다.
  - `4.5` **`PdfWriter.WriteAscii`의 `byte[]` 할당** — 지배적 비용은 토큰 인코딩이 아니라 **페이지
    래스터화**다(A4 150DPI 한 장 ≈ 8MB).
  - `4.6` **`ReplaceAll`의 O(N²)** — 지적은 맞으나 사용자 주도 단발 연산이고 루프 상한
    (`count <= 1_000_000`)이 이미 폭주를 막는다.

> ⚠️ **RTF 수정에서 한 번 틀렸다가 기존 테스트가 잡았다.** 첫 판은 `int.Parse`를 통째로 `TryParse`로
> 바꿨고, 그러자 **기존 6개가 빨개졌다** — `\fs99999999999999999999`가 이 리포의 "손상 RTF" 픽스처이자
> 진단 채널 테스트의 fault 원천이었기 때문이다. 옳은 수정은 파서 전체를 관대하게 만드는 것이 아니라
> **토큰화를 스펙에 맞추는 것**이었다: `-` 뒤에 숫자가 없으면 그건 파라미터가 아니다. 자릿수 초과는
> 그대로 던져 손상 판정을 유지한다. **동작을 바꾸려 할 때 무엇이 그 동작에 의존하는지 먼저 볼 것.**

#### 실기 확인이 잡은 결함 1건 — 쌍의 나머지 반쪽
**셀 이미지 우클릭은 실기에서 통과했고**(사용자 캡처: 이미지 메뉴 + 선택 핸들), 그 다음 클릭이 걸렸다 —
**"글자처럼 취급"이 아무 일도 안 했다.** 컨테이너 일반화를 `ConvertInlineImageToBlock`(inline→block)에만
넣고 `ConvertImageBlockToInline`(block→inline)을 빠뜨렸다. **같은 결함, 반대 방향.**
> 이 리포가 반복해서 밟은 함정이다(2026-08-07 `DrawCellBlockList`의 "같은 계열의 남은 한 쌍",
> R1의 "중첩 표 중복 조립을 최상위와 따로 찾아 따로 고쳤다"). **한 방향을 고치면 반대 방향을 함께 볼 것.**
> 그리고 이번에도 **자동 테스트가 아니라 사람이** 잡았다 — 컨텍스트 메뉴에서 명령까지의 배선은
> 포인터 주입이 없어 여전히 자동 검증 밖이다.

부수로 드러난 것: 셀 안 인라인 이미지의 폭 상한이 **문서 폭**이라 240px 이미지가 100px 열을 넘쳤다.
`ParagraphWrapWidth`(그리기·캐럿이 이미 쓰는 단일 출처)로 바꿨다.

**그리고 같은 함정이 한 번 더 있었다.** 2.14(하이퍼링크 대화상자)를 확인하러 갔더니 `ShowAsync()`가
가드 없이 호출되고 호출부는 전부 `_ = EditHyperlinkAsync()`였다 — 던지면 관측되지 않은 Task로 소멸한다
(2026-08-05에 툴바 파일 액션에서 이미 한 번 메꾼 구멍이다). 보고서는 하이퍼링크만 지적했는데,
**같은 파일 열 줄 아래 `EditImageAltTextAsync`도 똑같이 무방비**였다(그 아래 `SaveImageBytesAsync`는
가드가 있다 — 또 반쪽만 된 쌍). 둘 다 메꿨다.
> **이번 라운드에서 "쌍의 반쪽"이 세 번 나왔다**(셀 변환 방향, 대화상자 가드, 그리고 보고서 자신이
> `ConvertImageBlockToInline`을 못 본 것). 이 코드베이스에서 그건 우연이 아니라 **패턴**이다.

#### 감사 4.1 — 계측하고 고쳤다 (2026-08-26)
보류 목록에서 **1순위 후보**로 적어 둔 항목을 규칙대로 처리한 사례다: 먼저 재고, 숫자가 나온 뒤 고쳤다.

`TableBlock.Clone`/`Extract`가 공개 생성자에게 `rows×cols` 격자를 요구하고 다음 몇 줄에서 전부 버렸다
(슬롯마다 `TableCell`+`Paragraph`+`Run` 셋). `Clone`은 `UndoManager.PushState` 경로라 **언두 체크포인트
마다** 돈다. `Skeleton(rows, cols)`(크기만 선언, 격자 없음)을 두고 둘이 슬롯을 직접 채운다.

- **표가 지배적인 문서에서 clone 할당 −40%**(2000셀: 2606 → 1576KB), **일반 문서는 ~1%**(631 → 623KB).
- 감소분은 따로 값을 매긴 "버려지던 생성"과 1% 안쪽으로 일치. 동작·공개 표면 불변.

> **보고서의 "대량 GC 낭비"는 절반만 맞았다.** 낭비는 셀 수에 선형이라 일반 문서에서는 사실상 없다 —
> `PerfProbeTests`가 "현실적"이라고 부르는 모양이 정확히 그쪽이어서 **기존 프로브가 못 본 것도 그 때문**
> 이다. 그리고 무게는 시간이 아니라 **할당률**에 있었다(2000셀에서 키 입력 그룹마다 약 1MB의 쓰레기).
> ⚠️ **밀리초로 판단하지 말 것 — 이번에도 그 함정이 재현됐다.** 코드가 안 바뀐 대조군(표 없는 문서)이
> 두 실행 사이에 **16% 느려졌다**. 할당 바이트는 결정적이라 실행 간 비교가 성립하므로, 이 항목의
> 판정 근거는 전부 바이트다. **성능 A/B에서 코드가 안 바뀐 대조군 행을 반드시 함께 둘 것** —
> 그 행이 움직이면 시간 열 전체를 버려야 한다는 신호다.

하네스는 `PerfProbeTests`의 `CloneCostReport()`로 남겼다. 반증 확인: `Extract`의 행 구성 순서를
뒤집으면 기존 `Extract` 테스트 2개가 실패한다(즉 이 재작성은 미검증이 아니다). 퍼즈 4000시드 클린.

#### UIA 알림 — 실기 확인 완료 (2026-08-26), 그리고 이 리포 두 번째 컨트롤 계층 계측
"보조기술 클라이언트가 필요해서 자동 검증 불가"로 적었던 항목인데, **내레이터를 켜는 대신 UIA 클라이언트를
직접 붙이면 된다**는 것이 답이었다. 내레이터는 이 검증에 오히려 맞지 않는다 — **말로만 나가고 로그가 없어**
결과를 되읽을 수 없다. 클라이언트를 붙이는 것이 `ListenerExists`를 true로 만드는 조건 그 자체이기도 하다.

`tools/uia-probe`(구독·집계) + `tools/uia-probe/drive-demo.ps1`(실입력 `SendInput` 주입). **반증까지 완료**:

| 입력 | 수정 있음 | 수정 없음 |
|---|---|---|
| 화살표 9회 | Selection 9 / Changed 0 | 0 / 0 |
| 타이핑 3자 | Changed 3 / Selection 3 | 0 / 0 |
| 백스페이스 3회 | Changed 3 / Selection 3 | 0 / 0 |
| **합계** | **Changed 6 / Selection 15** | **0 / 0** |

> **얕은 확인은 고장난 빌드에서도 통과한다.** 수정을 뺀 빌드에서도 에디터는 UIA 트리에 정상으로 잡히고
> (`ControlType.Edit`, 이름 "Rich text editor"), **`TextPattern` 지원도 True고**, `DocumentRange` 텍스트와
> `GetSelection()`까지 다 나온다. "UIA에 노출되는가"를 물으면 **0/0인 빌드가 그 질문에 합격한다** —
> 갈리는 것은 오직 이벤트다. 히트테스트 폴백·`AdjacentTopLevelParagraph`와 같은 계열의 함정이다.
> **화살표가 Selection만 올리고 Changed는 안 올리는 것**도 함께 확인했다(둘을 무조건 올렸다면 여기서 갈린다).

> ⚠️ `SendKeys`가 아니라 **`SendInput`**이어야 한다(WinUI는 저널 방식 주입을 무시한다).
> 스크립트는 데모가 실제로 포그라운드일 때만 키를 보낸다 — 아니면 중단한다(다른 앱에 타이핑 금지).

#### 상류 수렴 — RTF 손상 판정 (2026-08-28)
**이번 감사 라운드에서 상류 대조를 빠뜨렸다가 뒤늦게 돌린 결과다.** CLAUDE.md가 공유 설계 변경 시 필수로
정한 단계인데 13건을 고치는 동안 한 번도 안 했다. 돌려 보니 셋이 나왔다:

- **RTF 파라미터 토큰화** — 상류가 **이미 고쳤고 계약이 달랐다.** 상류는 자릿수 초과를 "없는 파라미터"로
  넘기고 손상 신호를 남기지 않는다. 우리는 던져서 `TryParse`가 손상으로 보고했다.
  → **상류로 수렴**(상세는 CHANGELOG). 결정 근거는 **우리 쪽이 자기 원칙과 어긋났다는 것** —
  `\fs-x` 수정의 근거가 "완전한 문서를 토큰 하나 때문에 버리지 말 것"인데 자릿수 초과는 파일을
  **열지도 못하게** 했다. malformed 토큰 ≠ damaged 문서.
- **홀로 선 `-`** — 우리가 맞고 상류에 결함이 있었다 → **2026-08-28 백포트 완료.**
- **CSS 퍼센트 색상** — 상류 정규식도 `(\d+)`뿐이었고, 게다가 `int.Parse`라 `rgb(99999999999,0,0)`이
  **`OverflowException`을 `ParseHtml` 밖으로 던졌다**(색 하나가 아니라 붙여넣기 전체가 죽는다) →
  **2026-08-28 백포트 완료.**
- ~~**서로게이트 페어**~~ — **오진이었다. 상류는 이미 완전히 처리한다.**
  `PrevCharBoundary`/`NextCharBoundary`가 Backspace·Delete·←·→ **네 자리 전부**에 들어가 있고,
  다만 `IsHighSurrogate`/`IsLowSurrogate`를 쓴다. 내가 `IsSurrogatePair` **한 토큰으로 grep**하고
  "한 곳도 없다 → 미수정"이라고 단정했다.
  > ⚠️ **CLAUDE.md가 경고한 그 실수를 그대로 반복했다** — "공개 표면 대조는 눈대중 말고 기계적으로"는
  > 표면에만 해당하는 규칙이 아니다. **상류에 결함이 있다는 주장은 grep이 아니라 실행으로 확인할 것.**
  > 나머지 둘도 그래서 다시 돌렸고(상류 테스트로 재현), 둘 다 실재했다.

> **수렴이 드러낸 것: 이제 잘못된 RTF 중 던지는 입력이 하나도 없다.** 오버플로·잘못된 hex 이스케이프·
> 절단·범위 밖 색/`cellx`/글꼴 크기·과대 `\pict`를 전부 찔러 봤는데 모두 정상 문서로 읽힌다. 파서가
> 실제로 견고해진 것인데, **그 부작용으로 `RichEditorDiagnostics` 테스트 5개가 관측할 fault를 잃었다**
> (손상 RTF를 fault 원천으로 쓰고 있었다). `ColorUtil.ParseHex`로 옮겼다.
> **교훈: 테스트가 "던지는 입력"에 의존하면, 그 입력을 안 던지게 만드는 개선이 테스트를 무너뜨린다.**
> 진단 계약은 그것을 일으키는 특정 결함과 분리해 둘 것.

#### 실기 확인이 아직 필요한 2건 (자동 검증 불가)
- **디바이스 유실 후 이미지 재디코드** — 디바이스 유실을 합성할 수 없다.
- **대화상자 중복 시 가드** — `XamlRoot`가 null이면 `ShowAsync` 전에 반환하고, 진짜 모달을 띄우면
  테스트가 멈춘다.

### 리팩토링 R1 (2026-08-15) — 셀 걷기 통합, 완료
빌드 0/0, 테스트 **309 → 318**, 퍼즈 2000시드 클린, 실기 확인 완료.

**이 코드베이스의 리팩토링 여지는 "God object 해체"가 아니라 같은 걷기의 사본이었다.** 셀 블록 리스트를
걷는 코드 4개(측정·그리기·히트테스트·캐럿)가 advance 산술을 각자 갖고 있었고, 넷의 주석이 전부
*"draw walk와 일치해야 한다"* 고 적혀 있었다 — 코드가 지킬 수 있는 불변식을 사람에게 부탁하는 상태.
`NextCellSlot` 하나로 합쳤고, 문단 그리기(최상위/셀 2개)는 `DrawParagraphContent`로,
순서 목록 번호(2개)는 `OrderedStartFor`로 합쳤다.

> **가장 중요한 발견은 리팩토링이 아니라 반증이었다.** 공유 advance에 **3px 오차를 주입해도 309개가
> 전부 통과**했다(0으로 만들어야 4개 실패). **블록이 둘 이상인 셀은 그동안 0% 커버였다.**
> `CellWalkTests` 9개 신설.

> ⚠️ **폴백이 또 결함을 가렸다.** 신설 테스트의 첫 판도 주입한 divergence를 놓쳤다 — 히트테스트가
> 모든 블록을 빗나가면 **마지막 문단으로 폴백**하고, 마지막 문단을 겨냥한 probe에는 그 폴백이
> *정답을 틀린 이유로* 돌려준다. **문단 동일성만 단정하면 통과한다.** 오프셋까지 봐야 갈리고
> (진짜 히트=0, 폴백=문단 길이), probe도 캐럿 **중앙이 아니라 상단 +1px**이어야 한다(중앙은 8px 여유가
> 있어 작은 drift를 삼킨다). 2026-08-06 캐럿 라운드의 `AdjacentTopLevelParagraph` 폴백과 **같은 계열**이다.

- ⚠️ **동작 변경(경계 사례)**: 셀 안 순서 목록 사이의 비목록 블록(구분선·이미지·중첩 표)이 이제 번호를
  **1부터 재시작**시킨다. 최상위는 처음부터 그랬고 셀만 계속 셌다 — 규칙이 둘이었고 양쪽 다 테스트가
  없었다. HTML `<ol>` 규칙(=최상위)으로 통일.
- **부수 수정**: 중첩 표 기하가 프레임당 두 번 조립되던 것. 최상위의 똑같은 중복과 **따로 찾아 따로
  고쳐야 했다**.

### 리팩토링 R2 (2026-08-15) — 문서 순서 걷기 통합, 완료
테스트 **318 → 324**. 상세는 CHANGELOG.

문단 순서를 만드는 코드가 **넷**이었고 그중 **셋의 주석이 "저쪽을 미러링한다"** 고 적고 있었다
(`ParagraphsInBlocks` · `TextRange.CollectParagraphs` · `TextPointer.CompareTo` · `UndoManager.WalkParagraphs`).
R1과 정확히 같은 모양 — 코드가 지킬 수 있는 불변식을 사람에게 부탁하는 것 — 이고, **이미 한 번 깨졌다**
(TextPointer 주석: 재귀가 없어 인라인 표 안 포인터가 index −1로 남아 선택 비교가 뒤집혔다).
`Documents/BlockWalk.cs`가 소유한다. `TextRange.BlockHolds`(네 번째 사본)도 흡수. 전부 `internal`.

> **반증이 또 구멍을 열었다**: 인라인 표 하강을 빼면 기존 테스트 **1개**가 잡지만, **피복 셀을 순서에
> 넣어도 318개가 전부 통과했다** — 주석 셋이 load-bearing이라 적어 둔 `LogicalCells()` 규칙이
> 미검증이었다. `BlockWalkTests` 6개 신설(헤드리스).
> ⚠️ **그 신설 테스트의 첫 판은 공허했다** — `MergeCells`는 피복 셀 내용을 앵커로 **옮기고 빈 문단으로
> 교체**하므로, 병합 *전*에 잡은 객체가 순서에 없다는 단정은 문서에 그 객체가 없어서 통과한다.
> 병합 *후* 격자에 남은 문단을 단정해야 한다.

**성능**: `ParagraphsInBlocks`는 키 입력마다 문서 전체를 도는 경로라 iterator 레이어 추가분을
**같은 세션 연속 A/B**로 확인 — 공유 1.358ms vs 지역 사본 1.417ms, 측정 한계 이하.

#### 남은 후보 (근거와 함께 — 다시 파기 전에 이것부터)
- **R2 무리 B는 보류**(`CollectLiveImageKeys`·`CheckImageLimit.Count`·`HasAnyImage`·
  `UndoManager.EstimateBytes`·`NormalizeBlockList`). **차이가 의도적이다**: 무리 A는 전부
  `LogicalCells()`(피복 셀은 내용이 아니다), 무리 B는 전부 `tb.Cells`(**모든 슬롯** — 이미지 수집·바이트
  추산·부모 배선은 피복 셀 것도 봐야 한다). 플래그 하나로 합칠 수는 있으나 얻는 게 적다.
  `CaretParagraphsIn`도 제외 — 인라인 표에 **일부러 안 내려간다**(캐럿은 U+FFFC에서 나간다).
- **R3 — 기각.** "블록 레이아웃 맵을 `ComputePageBreaks`에 먹이자"는 **제안 자체가 틀렸다**:
  맵은 **폭 하나로만 캐시**되는데(`_blockLayoutWidth` 단일 값), 연속 모드의 `GetPrintPageCount()`는
  **용지 폭**으로 페이지를 나눈다(화면 맵은 뷰포트 폭). 맵을 쓰게 하면 **호출마다 맵이 두 폭을 오가며
  전 문서 재측정**된다. 지금 안 쓰는 게 맞다. 페이지 모드에선 폭이 일치하지만, 인쇄 정확성 경로에
  모드 의존 분기를 넣고 얻는 게 **공유되는 y 누산 6줄**이라 남는 장사가 아니다.
  (`PlaceAtom` 커버리지 구멍도 여전히 사실이지만, **더 정확한 기각 사유는 폭 축이다**.)
- **하지 말 것**: 히트테스트를 레이아웃에서 분리(비자명 규칙 #1의 정반대, 기각됨) · `RichEditor`의 클래스
  분해(파일은 이미 partial 27개다. 생산적이었던 방향은 6단계의 **순수 함수 추출**이다).

#### 성능 측정의 함정 (실제로 밟았다)
리팩토링 후 수치가 **2배 나빠 보였다**(assign 72 → 135ms). `git stash`로 **리팩토링 전 코드를 같은
시점에** 재보니 **똑같이 134~162ms** — 그 사이 기계가 느려진 것이었고 회귀는 없었다.
> **살아 있는 데스크톱에서 시점이 다른 성능 수치를 비교하지 말 것.** A/B는 반드시 같은 세션에서,
> 가능하면 연속으로. 그리고 측정 전에 **떠 있는 데모 프로세스를 죽일 것**(Win2D 공유 디바이스를 쓴다).

### 전수조사 (2026-08-15) — 용량·메모리·성능 축, 완료
빌드 0/0, 테스트 **306 → 309**, 퍼즈 2000시드 클린. 상세는 CHANGELOG.

**한 문장 요약: 이 축은 정독으로는 아무것도 안 나왔고, 계측 하나가 넷을 냈다.** 임시로 카운터를 심어
"키 하나에 `ParagraphSig`가 몇 번 도는가"를 세자 구조가 그대로 보였다(2000문단에서 **4017회** =
문서 전체 2패스). 측정 하네스는 `PerfProbeTests`로 남겼다(`RICHEDITOR_PERF=1`).

- [x] **문단별 곁캐시가 문서에서 빠진 문단을 붙잡고 있었다**(메모리, 유일한 실제 결함) —
      `Dictionary<Paragraph, …>`는 sig 검증 덕에 낡을 수는 없어도 **잊을 수는 없다**. `Document.Blocks`
      에서 뺀 문단이 **전체 GC 후에도 도달 가능**함을 측정으로 확인했다. 넷 다 `ConditionalWeakTable`로.
      > **핵심 계약**: 모델 객체를 키로 캐시를 만들면 **그 객체가 문서를 떠나는 경로**를 함께 볼 것.
      > sig 검증은 *정확성*만 지키고 *수명*은 지키지 않는다. 무거운 네이티브 자원을 담은 캐시는 예외 —
      > 명시적 `Dispose`가 필요하므로 강한 참조 + 상한 + 축출로 남긴다(`_layoutCache`).
- [x] **측정 전용 레이아웃이 색·밑줄·취소선을 만들고 있었다** — DirectWrite는 그걸로 *그리지* 배치하지
      않는다. 문서 열기 **84.9 → 72.2ms**(2000문단). 전제는 `MeasureLayoutMatchesDrawLayout`이 고정.
- [x] **프레임당 낭비 2건** — 목록이 없는 문서에서도 보이는 문단마다 `BuildPlain`, 표 기하를 프레임당
      두 번 조립. 페이지 렌더 **1.18 → 0.83ms**.
- [x] **게시 다이어트 3단계** 78.0 → **74.4MB** — 안 쓰는 언어 `.mui` 84종 + 라이브러리 `.xml`/`.pdb`.

#### 측정된 한계 (재조사 방지)
- **AOT 네이티브 exe 14.5MB는 거의 바닥이다.** `IlcOptimizationPreference=Size` +
  `UseSystemResourceKeys` + `IlcFoldIdenticalMethodBodies` + 기능 스위치 4종을 전부 켜도
  **14.54 → 14.11MB(3%)** 뿐이고, `UseSystemResourceKeys`는 예외 메시지를 리소스 ID로 바꿔
  `RichEditorDiagnostics`의 보고를 못 쓰게 만든다. **0.4MB에 팔 물건이 아니다.**
- **`.winmd` ~2.7MB는 지워도 기동·렌더된다** — 그러나 WinRT 메타데이터는 활성화 시점에 지연 해석되므로
  첫 화면은 파일 피커·인쇄 대화상자에 대해 아무 증거가 아니다. **측정된 후보로만** 남긴다.
- **타이핑 지연은 문서 길이에 선형이고 그 기울기는 `ParagraphSig`다**(키당 문서 2패스: 블록 레이아웃
  맵 + `GetStatus`). Release 2000문단에서 **0.73ms**라 지금은 문제가 아니지만 2만 문단이면 체감 영역에
  들어간다. sig를 메모이즈하면 없앨 수 있으나 **모든 곁캐시의 무결성이 그 메모 하나에 걸리고**,
  호스트가 모델을 직접 고쳐도 다음 그리기에서 잡히는 현재 성질을 잃는다 — 실수요 전엔 하지 않는다.
- **문서 열기 비용의 대부분은 네이티브 `CanvasTextLayout` 생성**(2000문단 72ms ≈ 36µs/문단)이고,
  스크롤바를 재려면 전 문단을 재야 한다. 없애려면 화면 밖 문단을 **추정**하고 나중에 보정해야 하는데
  스크롤 썸이 떨리는 UX 트레이드오프라 별도 판단 사항.

### 전수조사 (2026-08-07) — 완료, 결함 **12건** + 커버리지 3건
빌드 0/0, 테스트 **265 → 294**, 퍼즈 20000시드 클린. 상세는 CHANGELOG.

**두 국면이었다.** 앞의 6건은 퍼즈/행렬이 잡았고(아래), 뒤의 6건은 **사용자 실기 검증 3회**가 잡았다 —
RTF 마커 주입 · 셀 문단 속성 누락 · 문단 배경 미기록 · 표 자동 맞춤 · 줄간격 미지정 · 목록 내어쓰기 없음.
> **관측**: 뒤의 6건은 전부 **모델이 아니라 "포맷이 소비자에게 말하는 내용"** 의 문제였다. 우리 왕복
> 테스트는 전부 통과하면서 Word/HWP에서만 틀렸다. 그래서 그 6건의 테스트는 **쓰여진 바이트**를 단정한다.
> 왕복 대칭성만 보는 테스트는 이 계열을 원리적으로 볼 수 없다.

**한 문장 요약: 퍼즈가 서식을 만들기만 하고 보지 않았다.** `case 3`/`4`/`12`가 처음부터 굵기·색·링크·
제목·인용·들여쓰기·목록·줄간격·셀 배경을 넣어 왔는데 `Shape()`가 그중 **아무것도** 기록하지 않았다.
2026-08-06 라운드의 "생성과 관찰은 짝" 규칙이 **반대 방향으로** 깨져 있던 것이고, 짝을 맞추자 결함 5건이
나왔다. 안 만들던 축(이탤릭·밑줄/취소선·글꼴·크기·정렬·마커·여백·대체 텍스트)도 생성에 넣었다.

**두 번째 수단이 더 중요했다 — 왕복 멱등성은 "매 회차가 똑같이 버리는 것"을 원리적으로 못 본다.**
JSON/`.flow`는 자체 저장 형식이라 무손실이어야 하므로 **원본과 대조**하는 판을 새로 뒀다
(`RandomDocuments_SurviveTheNativeFormatsWithoutLoss`). HTML·RTF는 문서화된 손실이 있어 멱등성만 적용한다.
> **핵심 계약**: 멱등 테스트는 *누적*을 잡고 *균일한 손실*은 못 잡는다. 무손실이어야 하는 포맷에는
> 원본 대조를 따로 둘 것 — 셀 문단 배경(결함 1)은 1회차부터 사라져서 멱등 테스트에 완벽히 투명했다.

- [x] **셀 안 문단 배경색이 JSON/`.flow`에서 소실** — 레거시 한-문단 셀 인코딩이 `Background` 하나를
      셀의 채움과 문단의 채움 둘에 공유하고 셀 대입이 나중에 온다. 문단이 채움을 가질 때만 `"Cell"`
      래퍼를 쓴다(형식 불변, 리더 변경 없음).
- [x] **HTML: 셀 문단이 맨 인라인 + `<br>`로 나가 문단 서식 전멸** — 클립보드 HTML도 같은 경로라
      글머리표 셀이 Word에서 그냥 줄이 됐다. **리더는 원래 할 수 있었다**(쓰는 쪽만 못 했다).
- [x] **HTML: `<br>`가 문단 경계가 아니라 두 문단 셀이 한 문단으로 붕괴** — **붕괴가 멱등**이라
      왕복 퍼즈에 투명했다. 문단 둘 이상인 셀은 전부 요소로.
- [x] **HTML: 그림이 든 문단이 문단 서식 전멸**(최상위에서도) — `HasBlockOrMedia`가 `BlockLeaf`보다
      먼저 와서 `<img>` 하나에 `<p style>`이 컨테이너가 됐다. **미디어만** 든 경우로 좁혀 고쳤다.
- [x] **HTML: 문단 여백 3종을 아예 쓰지 않아 저장마다 기본값으로 초기화** — CSS + `data-are-m` 마커,
      읽기는 마커만(외부 여백을 읽으면 웹 붙여넣기가 그 페이지의 세로 리듬을 들고 온다).
- [x] **PDF 내보내기 구조 검증 신설** — `PdfWriter`는 테스트가 0개였다. xref 오프셋·20바이트 고정폭·
      `startxref`·스트림 `/Length`·`/Kids`. 반증 확인 완료(오프셋 +1 심으면 2개 실패). **결함은 없었다.**
- [x] **퍼즈 시그니처를 런 경계에 무관하게** — 같은 서식의 인접 런 병합은 로더가 의도적으로 하는
      정규화(`RunNormalizer`)인데 시그니처가 그걸 차이로 보고했다(20000시드에서 12건). 이런 위양성이
      진짜 발견을 묻는다.

#### 실기 확인이 잡은 결함 1건 + 렌더 픽셀 테스트 신설
위 수정들을 데모에서 보려고 **셀 서식 표를 데모 샘플에 넣자마자** 나왔다: 문단 배경이 모든 포맷을
왕복하고 모델에도 있는데 **화면에 아무것도 안 나왔다.** `DrawCellBlockList`가 런 배경·목록 마커·
하이라이트·선택·텍스트는 그리면서 **문단 배경과 인용 막대는 그리지 않았다**(최상위 루프는 둘 다 그린다).
같은 파일 주석이 목록 마커에 대해 똑같은 누락을 기록해 뒀다 — **같은 계열의 남은 한 쌍**이었다.
> **핵심 계약**: 문단 레벨 속성을 새로 살리면 **최상위 루프와 `DrawCellBlockList` 둘 다** 볼 것.
> 두 경로가 갈라져 있고, 모델·포매터 테스트는 원리적으로 이 차이를 볼 수 없다(모델은 틀린 데가 없다).

그리고 이걸 계기로 **이 리포 최초의 렌더 픽셀 테스트**를 세웠다(로드맵이 꼽아 온 격차: 상류 17개 vs 포트 0개).
길은 `RenderPrintPage`다 — `CanvasDevice.GetSharedDevice()`에 오프스크린으로 그리므로 **UI 스레드는
필요하지만 비주얼 트리는 필요 없다**. `RenderPixelTests` 4개, 반증 확인 완료(그리기 2개를 되돌리면
해당 2개만 실패, 대조군 2개는 통과).
> ⚠ **데모 캡처의 한계(실측)**: 창을 작업 영역보다 크게 만들 수 없다 — `SetWindowPos`/`MoveWindow`로
> 높이 2300, 음수 Y까지 시도했지만 **1084로 클램프**된다. 그리고 WinUI는 blind SendKeys를 막아 스크롤도
> 못 한다. 즉 `--page=` + PrintWindow 레시피는 **첫 화면만** 검증할 수 있다 — 확인이 필요한 내용은
> 데모 샘플의 앞쪽에 둘 것(이번에 그렇게 옮겼다).

#### 사용자 실기 검증이 잡은 결함 — RTF 목록 마커가 내용에 주입되던 것
**Word·HWP·브라우저 붙여넣기를 사람이 확인한 결과가 이걸 열었다.** HWP 캡처에 `■`가 딴 줄에 혼자
떠 있었고, 원인을 따라가니 RTF writer가 마커를 **리터럴 텍스트 + `\tab`** 으로 쓰고 있었다. 그래서
`.rtf`로 저장→열기가 목록 항목을 평문으로 만들면서 **글리프를 사용자 텍스트에 주입**했다
(`P{list=Bullet}[항목]` → `P{list=None}["•\t항목"]`). 손실이 아니라 **내용 주입**이다.

> **퍼즈가 왜 못 봤나**: 결과가 **완벽히 멱등**이다. 2회차가 1회차와 글자 단위로 같다 — 마커는
> `ListType`이 남아 있는 문단에만 쓰이고 그 문단은 이제 `ListType`이 없기 때문이다. 이 세션이 세운
> 원본 대조 판도 JSON/`.flow`만 덮었다(RTF는 "문서화된 손실"로 제외 — 주입일 줄은 몰랐다).
> **교훈**: "문서화된 손실"로 제외한 포맷도 **손실인지 변조인지**는 확인해야 한다.

해법: **마커를 일반 텍스트로 두고 그것이 구조임을 알리는 자체 태그**를 앞에 붙인다 —
`{\*\armkb<code>}`/`{\*\armkn<code>}`. 무시 가능 그룹이라 **다른 모든 리더가 건너뛰고 뒤 텍스트를 그대로
렌더**하며, 태그는 우리 리더에게 "다음 `\tab`까지는 마커"라고 말하고 파라미터가 정확한 스타일을 싣는다.
`{\*\arinline}`과 같은 idiom. `ListType`은 물론 **마커 스타일까지**(▪ vs •, `1)` vs `1.`) 복원된다.

> ⚠️ **실기가 설계를 한 번 되돌렸다.** 처음엔 표준 `{\pntext}{\*\pn}` 쌍을 썼고 Word는 진짜 목록을
> 만들었지만 **HWP는 둘 다 무시해서 마커가 통째로 사라졌다**(사용자 캡처). 한쪽에서 사라지는 것보다
> 양쪽에서 리터럴로 보이는 것이 낫다 — 이 트레이드오프는 의식적으로 택한 것이다.
> **읽는 쪽의 `{\*\pn}` 지원은 유지**했다: Word가 그렇게 쓰므로 들어오는 Word 목록이 `ListType`을
> 얻는다(실측 확인). **HWP는 RTF 목록을 구현하지 않는다** — 중첩 표와 같은 계열의 사실이다.
> 리더의 마커 삼킴 상태는 `\par`/`\pard`에서 반드시 해제된다(손상 입력이 내용을 삼키지 못하게).

#### 측정된 포맷 손실 (속성 × 포맷 × 위치 행렬, 재조사 방지)
런 9종 + 문단 12종을 최상위와 셀 안에서 네 포맷에 통과시켜 **측정**했다. 위 5건을 고친 뒤 남은 것:
- **JSON/`.flow`: 손실 없음**(측정한 전 항목 보존). 무손실 계약이 이제 퍼즈로 지켜진다.
- **HTML**: 문단 속성 전부 보존(최상위·셀 동일). `PageSetup`과 `RowHeights`는 미보존.
- **RTF**: 런 서식 9종 전부 보존. **목록은 종류와 마커까지 보존**(아래 수정 전엔 버렸고, 그보다 나빴다).
  정렬·들여쓰기·줄간격은 **최상위와 셀 양쪽에서** 보존(아래 수정 전엔 셀에서만 버렸다).
  남은 손실: **제목·인용·문단 배경·`ListLevel`** — 이 넷은 최상위·셀 동일하므로 진짜 표현력 한계다.
  이미지 **대체 텍스트**와 `PageSetup`도 RTF에 자리가 없다.
  > ⚠️ **이 줄의 이전 판이 틀렸다.** "RTF의 문단 레벨 손실은 최상위·셀이 동일하므로 일관된 표현력
  > 한계"라고 적었는데, **같은 행렬의 내 데이터가 그걸 반박하고 있었다** — `para.align`/`indent`/
  > `lineSpace`/`lineHeight`는 `top=ok, cell=≠`였다. 제목·인용에서 본 대칭을 문단 속성 전체로
  > 일반화해 버린 것이고, 그 네 줄이 전부 **셀 writer의 하드코딩 `\ql` 하나**였다(아래 수정).
  > **교훈**: 행렬을 요약할 때 요약이 각 줄과 맞는지 다시 볼 것 — 비대칭 한 칸이 곧 누락의 증거다.

#### 상류 백포트 후보 (전부 대조로 확인 — 상류에 그대로 있음)
- **셀 문단 배경 소실**: 상류 `DocumentSerializer.cs:192` `cdto.Background = BrushToString(...)`와 읽는 쪽
  `para.Background = null`이 **글자 그대로 동일**하다. `"Cell"` 래퍼도 상류에 있어 `.flow` 호환 유지된다.
- **HTML 셀 문단 서식/붕괴**: 상류 `firstCellPara` 판은 포트가 이미 고친 개선(“앞 블록이 문단일 때만
  `<br>`”)조차 없어서 **중첩 표·이미지 뒤에도 `<br>`가 붙는다** — 포트보다 한 단계 더 나쁘다.
- **그림이 든 문단의 서식 소실**: 상류 리더도 `HasBlockOrMedia` → `BlockLeaf` 순서가 동일하다.
- **문단 여백**: 상류도 HTML에 쓰지 않는다.
- **RTF 목록 마커 주입**: 상류도 `RtfDocumentFormatter.cs:858`/`1015`에서 마커를 리터럴 텍스트로 쓰고,
  리더가 마커를 구조로 인식할 수단이 없다 — writer의 태그와 리더의 삼킴 상태를 함께 옮겨야 한다
  (상류의 `pntext` 스킵만으로는 HWP에서 마커가 사라지는 쪽이 된다 — 우리가 그 판을 실기로 되돌렸다).
- **셀 문단 속성 누락**: 상류는 더 나쁘다 — 셀을 `\pard\intbl`로만 열고(`:955`/`962`/`991`) 정렬도
  `\itap`도 안 쓴다. 즉 셀 문단의 정렬·들여쓰기·줄간격이 전부 빠진다.

#### 이번 라운드에서 확인만 하고 고치지 않은 것
- **상류의 뷰어 이미지 복사 수정(`ec73920`)은 백포트 대상이 아니다** — 상류가 포트에 맞춰 고친 것이고,
  포트는 `ChooseContextMenu`가 객체를 읽기 전용보다 먼저 두고 이미지 메뉴가 편집 동사를 전부
  `!IsReadOnly`로 게이트한다(대조로 확인). 뷰어에서 저장(`ImageSaveHandler`)만 열려 있는 것은 의도다.
- **`RichEditorToolbar.Target`/`ShowFileActions`, `RichEditorView.ShowStatusBar` 등이 CLR 속성**(상류는 DP).
  XAML 바인딩이 안 되는 실제 격차지만 코드 전용 라이브러리라는 설계와 API 동결 때문에 **추가**로만
  가능하다(`*Property` 신설은 non-breaking) → **1.1 후보**. 기계적 표면 diff로 확인했다.

#### 실기 검증이 남긴 한계 3건 (2026-08-07, 재조사 방지)
사용자 왕복 검증 3회로 결함 6건을 더 잡고 남은 것들. **고칠 수 없거나, 우리 쪽 문제가 아니다.**
- **HWP가 구분선을 렌더하지 않는다** — 우리는 "아래 테두리를 가진 빈 문단"으로 쓰고 **Word는 정상 표시**
  한다(캡처 확인). RTF에 가로선 제어어가 따로 없어 이게 표준 관용이다. **HWP의 RTF 미구현 계열**이고,
  중첩 표(위 절)와 같은 성격이다.
- **HWP 줄간격이 편집기와 다르다** — 미지정 문단을 `\sl240\slmult1`(단일)로 **명시**하도록 바꿨는데도
  다르다. HWP의 100%는 글자 크기 기준이라 자연 행간보다 좁다(HWP 기본은 160%). 어느 값이 편집기와
  맞는지는 HWP를 봐야 정할 수 있어 **현재 값을 유지하고 알려진 차이로 남긴다**(사용자 결정).
- **브라우저의 다열 레이아웃은 한 열로 펼쳐진다** — 결함이 아니라 HTML의 성질이다. 요즘 웹의 여러 열은
  `<table>`이 아니라 CSS grid/flex `<div>`이고, **마크업에 "열"이라는 구조가 없다**(배치는 CSS에만 있다).
  복원하려면 CSS 레이아웃 엔진이 필요하고, Word를 포함한 모든 에디터가 똑같이 펼친다.

> **Word에서 문단 서식이 안 들어오는 건 우리 데이터 문제가 아니다** — 실제 클립보드 바이트를 덤프해
> 정렬(`\pard\qc`)·배경(`\cbpat`)·표 너비(`\trautofit0\trftsWidth3\trwWidth`)가 **모두 들어 있음을
> 확인**했고, HWP는 같은 바이트로 배경을 받는다. 증상이 **문자 서식은 살고 문단 서식만 통째로 죽는**
> 형태인데, 그것이 정확히 Word의 **"서식 병합"** 붙여넣기 동작이다(표가 용지 폭으로 늘어나는 것도 같은
> 원인 — 대상 표 스타일). 다음에 같은 리포트를 받으면 **붙여넣기 옵션부터** 확인할 것.

### 알려진 한계 (1.0 시점, 의도적)
- **HWP는 RTF 중첩 표를 구현하지 않는다** — `
estcell`을 버리고 중첩 셀 텍스트를 이어 붙인다.
  우리 출력은 원본 1.0과 **바이트 단위로 동일**하고 Word는 정확히 읽는다. 포트 결함이 아니다.
- 모든 행이 똑같이 병합된 표는 RTF에서 **한 열로 접힌다** — 파일에 원래 격자 단서가 없다(렌더 동일).
- 중첩 표의 **열 너비**는 무시 가능 그룹에 있어 기본값으로 들어온다.
- ~~병합 셀 안 공백 하나가 2회째 HTML 왕복에서 소실~~ → **2026-08-06 수정**(위 전수조사 절).

### 재사용할 검증 레시피
- ⚠️ **파일 피커로 열 테스트 파일을 `%TEMP%` 밑 스크래치패드에 두지 말 것.** WinRT `StorageFile`이
  그 경로를 거부해 `PickSingleFileAsync()`가 **`COMException 0x80004005`(E_FAIL)** 로 던진다 — 대화상자가
  아예 안 뜨므로 "파일이 안 열린다"로 보이고, 원인이 파일 내용에 있는 것처럼 오해하게 된다.
  픽스처는 **`Documents`** 에 두면 된다(피커 기본 시작 위치이기도 하다). 2026-08-05에 이걸로 왕복 8번을
  낭비했고, 그 사이 HWND·권한 상승·OneDrive 리디렉션·셸 라이브러리를 전부 헛짚었다.
- **데모 빌드 순서**: `dotnet build WinUIRichEditor.slnx` **먼저**, 그 다음 데모 csproj. 라이브러리는
  AnyCPU(`bin/Debug`)와 x64(`bin/x64/Debug`) 양쪽에 나오는데 **데모는 AnyCPU 쪽을 참조**한다. 데모
  csproj만 빌드하면 "성공"이라 보고하면서 옛 라이브러리를 그대로 들고 간다 — 반드시 데모 출력 폴더의
  `WinUIRichEditor.dll` **타임스탬프로 확인**할 것.
- **AOT 동등성 스윕**(`tools/fault-sweep.ps1`, 2026-09-06 신설) — **AOT는 렌더되지만 동등하지 않을 수 있다**는
  것이 1.1.0이 깨진 채 나간 이유다(줄 메트릭). 같은 편집 순서를 **JIT와 AOT 게시본에 각각** 돌려
  ① 라이브러리가 보고한 fault 집합 ② `Ctrl+A`/`Ctrl+C`로 되읽은 **문서 텍스트**를 비교한다.
  데모의 `--faultlog=<경로>`(App 레벨 구독)가 채널이고, 삼킴 지점은 **전부 보고**하므로 로그가 곧
  "발동한 폴백의 집합"이다(진단은 지점당 1회 보고).
  ```
  dotnet publish samples/.../WinUIRichEditor.Demo.csproj -c Release -r win-x64 --self-contained true \
    -p:Platform=x64 -p:PublishAot=true -p:PublishTrimmed=true
  tools\fault-sweep.ps1 -Exe <jit exe> -Log C:\tmp\faults-jit.txt   # 대조군
  tools\fault-sweep.ps1 -Exe <publish\...exe> -Log C:\tmp\faults-aot.txt
  ```
  **2026-09-06 실측**: AOT 전용 fault는 `LineMetricsOf`(non-blittable 배열, 폴백 정상 발동) **하나뿐**이고
  문서는 `control`·`toolbar` 두 페이지에서 **바이트 동일**(711자). 다른 조용한 AOT 저하는 없었다.
  > ⚠️ **키만 주입하면 아무 데도 안 간다.** 데모의 컨트롤 페이지는 `FocusEditor()`를 부르지 않아
  > 클릭 전엔 캔버스에 키보드 포커스가 없다 — 첫 두 번의 스윕이 **입력 0인 세션을 재고도 "fault 없음"으로
  > 정상처럼 보였다**(렌더만으로도 AOT fault는 뜬다). 그래서 phase 0에 **마우스 클릭 주입**이 있다.
  > 되읽은 문서가 비면 비교는 **공허하다** — 스크립트가 그 경우 경고를 찍는다(빈 파일 둘은 "동일"하다).
  > `tools/uia-probe/drive-demo.ps1`에는 아직 이 클릭이 없다 — 그 스크립트의 숫자는 누군가 먼저
  > 클릭했다는 전제에 기대고 있다.
- **UIA 알림 측정**(`tools/uia-probe`) — 스크린 리더가 캐럿·텍스트 변경을 따라오는지를 **숫자로** 본다.
  데모 실행 → `dotnet run --project tools/uia-probe -- 22` → 다른 셸에서 `tools/uia-probe/drive-demo.ps1`.
  솔루션에 없으므로 CI는 건드리지 않는다. **내레이터로는 이 검증을 못 한다**(소리뿐, 로그 없음).
  ⚠️ "UIA에 노출되는가"만 보면 **알림이 하나도 없는 빌드도 통과한다** — 반드시 이벤트 수를 볼 것.
- **캐럿 깜빡임 측정**(`tools/measure-caret-blink.ps1`) — 이 리포 **최초의 컨트롤 계층 자동 검증**이다.
  창을 60ms 간격으로 `PrintWindow` 캡처해 픽셀 변화 간격을 잰다. "눈으로 보고 판단"을 숫자로 바꾼다.
  깜빡임 켬 → 평균 간격 ≈ `CursorBlinkRate`, 끔 → 변화 0회 **+ 캐럿은 계속 보여야 한다**(설정이 끄는
  것은 *움직임*이지 캐럿이 아니다. 사라지면 결함).
  ⚠️ **변화 0회는 단독으로 증거가 아니다** — 포커스 없는 에디터는 캐럿을 안 그려서 역시 0이 나온다.
  반드시 정상 속도에서 대조군을 먼저 돌려 `CursorBlinkRate` 근처 값이 나오는 것을 확인할 것.
  설정 변경은 **`control keyboard`의 슬라이더**로 한다(즉시 반영). 레지스트리를 직접 고치면
  **재로그인해야** `GetCaretBlinkTime`이 새 값을 준다(USER가 세션 시작 시 캐시). 컨트롤은 **포커스를
  얻을 때** 다시 읽으므로, 설정만 바꿨다면 에디터 밖을 클릭했다 다시 들어와야 한다.
  더블클릭은 `control main.cpl` — 단어 선택이 되는지로 바로 보이므로 측정까지는 불필요하다.
  **2026-08-05 실측**: 대조군 ≈530ms, 끔 → 변화 0회 + 캐럿 유지. `11379da` 검증 완료.
- **데모 GUI 확인**: `--page=control|readonly|toolbar|view`로 시작 페이지 지정 후 `PrintWindow`
  (`PW_RENDERFULLCONTENT`)로 캡처. ⚠ 데모 exe는 `bin\x64\Debug\...`에 나온다(`bin\Debug\...`의
  옛 exe를 잡으면 어제 빌드를 띄운다).
- **NuGet 소비자 스모크**: 아래 "1.0.0 릴리스" 절에 스캐폴드 레시피와 함정 3가지.
  ⚠️ 함정 ④(1.1.1에서 밟음): **패키지의 XML 문서는 소비자 출력 폴더로 복사되지 않는다**
  (`CopyDocumentationFilesFromPackages` 기본 off — IntelliSense는 패키지 폴더에서 읽는다).
  출력에서 찾으면 문서를 담고 있는 패키지가 "문서 없음"으로 실패한다. **복원된 패키지**에서 볼 것.
- **테스트 실행**: `dotnet test <csproj>`(빌드 포함). `--no-build`는 다른 출력 경로의 스테일 DLL을 돌린다.
- **창 캡처**: `tools\capture-window.ps1 -ProcessName WinUIRichEditor.Demo -Out shot.png`.
  WinUI 3는 일반 `WM_PRINT` 밖에서 합성하므로 `PW_RENDERFULLCONTENT`(플래그 2)여야 캔버스가 나온다.
- **성능 측정**: `$env:RICHEDITOR_PERF=1` + `dotnet test ... --filter PerfProbe` → `perf-report.txt`
  (경로는 `RICHEDITOR_PERF_REPORT`). **Release로 돌릴 것** — Debug는 절대값이 부풀고, 무엇보다
  `WeakReference` 계열 판정이 지역변수 수명 때문에 다르게 나온다.
  ⚠️ **성능 가설은 계측으로 먼저 확인할 것.** 이번 라운드에서 유력해 보이던 후보 몇 개는 임시 카운터를
  심자마자 무관한 것으로 드러났고, 실제 비용은 세어 보기 전엔 이름조차 나오지 않았다.

## 결정 사항 (고정)
- **줌은 엔진이 인다**(상류는 뷰의 `LayoutTransformControl`이 인다) — **수렴하지 않는다.**
  Win2D `CanvasVirtualControl`은 즉시모드 **래스터** 표면이라 위에 XAML 변환을 걸면 *이미 래스터화된
  출력*을 확대해 200/300%에서 글자가 뭉갠다. 크리스프하려면 스케일이 `CanvasDrawingSession`까지
  내려가야 하고(`ds.Transform`), 그 순간 엔진이 줌을 알아야 한다. 대가는 `EffectiveZoom` 15곳 +
  `ViewToDoc`/`DocToView` 16곳이고, 그 대가를 **2026-09-08 `ControlZoomTests`로 갚았다**.
  상류는 프레임워크가 좌표 변환을 해주므로 그쪽 방식이 그쪽에서 옳다 — 어느 쪽으로 통일해도 한쪽이
  나빠진다. (상류 방식의 대가는 툴바의 훅 5개와 **맨 `RichEditor`엔 줌이 없다**는 점이다.)
- **문자 토글(굵게·기울임·밑줄·취소선)은 Word 방식**(2026-09-12, 사용자 결정) — 대상 글자가 전부 이미
  그 서식이면 끄고, 아니면 모두 켠다. 런마다 뒤집던 옛 동작(상류는 아직 그렇다)으로 되돌리지 말 것.
  수렴은 **상류가 이쪽으로** 오는 방향이다.
- **직접 지정한 글자 크기는 제목 수준을 바꿔도 유지된다**(2026-09-12, 사용자 결정) — 제목을 따르는 것은
  서식 없는 글자(크기 미지정 또는 본문 기본 10pt)뿐이다. `SetHeading_ResizesUnstyledText_AndKeepsAnExplicitSize`가 고정.
- **이미지·표를 끈 에디터에 들어오는 내용은 적응한다**(2026-09-12, 사용자 결정) — 붙여넣기(모든 리치 형식)와
  `InsertHtml`에서 이미지는 버리고 표는 셀 글자로 푼다. 이미 문서에 있는 내용(텍스트 드래그)과 문서 열기(`Load*`)는
  적응하지 않는다. `AllowImages`/`AllowTables`는 **추가**를 막는 플래그다.
- **문서가 아닌 입력은 거부하고 열린 문서를 지킨다**(2026-09-12, 사용자 결정) — 에디터의 `LoadJson`/`LoadJsonAsync`/
  `LoadPackageAsync`는 `Blocks` 없는 JSON 객체를 `JsonException`으로, `document.json` 없는 zip을 `InvalidDataException`으로
  거부한다. 리터럴 `null`만 빈 문서다. 빈 문서로 읽으면 "저장됨"이 되어 다음 저장이 원본을 덮는다.
- **페이지 설정은 호스트 기본값에서 시작한다**(2026-09-12, 사용자 결정) — 설정 없는 문서·새 문서는 호스트가 코드로 정한
  값(속성 단위 기록)에서 시작하고 직전 문서의 값을 물려받지 않는다. 툴바의 용지·방향 선택은 열린 문서의 편집이다.
- **타이핑한 글자의 서식은 캐럿 앞의 가장 가까운 글자에서, 이미지는 건너뛰고, 없으면 뒤의 글자에서**(2026-09-12,
  사용자 결정 · Word 방식) — 삽입(`TryInsertTextCore`)과 캐럿 보고(`CaretFormatRun`)가 `TypingSource` 하나를 쓴다.
  둘을 따로 두지 말 것: 따로 있던 동안 이미지 옆 4개 위치에서 툴바와 타이핑이 달랐다.
- **렌더러가 강제하는 서식은 그려진 대로 보고하고, 그것에 대한 토글은 아무것도 하지 않는다**(2026-09-12, 사용자 결정) —
  제목의 굵게, 링크의 밑줄·파랑. 대상이 전부 강제 서식이면 Ctrl+B/U는 변경도 언두 단계도 없다. 섞여 있으면 강제 서식은
  "켜진 것"으로 친다(Word 규칙의 판단 입력).
- **링크 끝·시작에서 친 글자는 링크가 아니다**(2026-09-12, 사용자 결정 · Word 방식) — 링크는 양쪽 글자가 같은 링크일 때만
  (링크 안에서만) 이어진다.
- 렌더 백엔드: **Win2D** (`CanvasTextLayout`이 Avalonia `TextLayout`과 1:1, AOT는 CsWinRT 2.2.0 레일).
- 패키징: **Unpackaged** (MSIX 아님).
- 목표: **Native AOT** 퍼블리시 가능.
- 컨트롤은 XAML 없는 코드 전용.

## 1.0 직전 2차 대조 + 전수조사 (2026-07-31) — 완료
동결 전 한 번 더. 상류는 `v1.0.0`에서 멈춰 있고 작업 트리도 깨끗 — 새로 들어온 것 없음.
**결함 4건 추가 발견·수정.** 빌드 0/0, 테스트 **91/91**, AOT 게시 재검증 완료.
- [x] **동작 플래그 8개가 더 CLR 속성**(`Allow*` 6 + `MaxRecommendedImages` + `ShowFormattingMenu`).
      **교훈**: 트랙 E에서 "`AutoLinkOnType`만 그렇다"고 판단한 근거가 틀렸다 — 공개 표면 대조는
      눈대중이 아니라 **원본 `PublicAPI.Shipped.txt`와 기계적으로** 맞출 것(그 스크립트가 이걸 잡았다).
- [x] **중첩 표 누산기가 표 사이에 누수**(이번 세션이 넣은 RTF 표 모델의 결함). `_nestRow`/`_nestRows`/
      `_cellPending`이 안 비워져, 끊긴 입력의 depth 2 잔여를 **다음 표의 첫 셀이 주워 갔다**.
      `FinalizeTable`에서 정리. **핵심 계약**: 최상위 표가 끝나면 중첩 상태는 전부 소비됐어야 하므로
      남은 것은 고아다 — 깊이 키 누산기를 새로 만들면 경계에서 비울 것.
- [x] **찾기 바 닫으면 캐럿이 죽던 것**(트랙 C의 커버리지 구멍 — 툴바만 보고 찾기 바를 안 봤다).
      `Editor.Focus()`는 컨트롤이 탭 스톱이 아니라 안 닿는다 → `FocusEditor()`.
      **재발 방지**: `RichEditor`에 포커스를 주는 코드는 **반드시** `FocusEditor()`를 쓸 것.
- [x] **찾기 바 버튼이 포커스 강탈**(다음 Enter가 검색 대신 버튼 재실행) → 검색 상자에 포커스 유지.
- [x] **포매터 클래스 문서가 실제 동작과 불일치**(XML 문서가 이제 배포되므로 소비자에게 그대로 간다)
      + 깨진 문자 4곳(`—`→`??`, 세션 이전부터 있던 것). 둘 다 수정.

## 1.0 직전 RTF 표 모델 테스트 보강 (2026-07-31) — 완료
동결 전 마지막. 표 모델은 이 영역 최대 변경이고 붙여넣기 경로에 있는데 축별 테스트가 **축을 겹친
조합**을 못 덮고 있었다. 테스트 **91 → 101**, 결함 1건 추가.
- [x] **셀 안 중첩 표 앞 문단에 왕복마다 개행 증식** — 쓰는 쪽은 하강 전 `\par`가 **필수**인데(Word 요구)
      읽는 쪽은 셀 안 `\par`를 내용으로 읽어, 구분자가 내용이 되고 다음 왕복에서 다시 쌓였다.
      깊이 전환 지점에서 후행 개행 하나를 떼어낸다(`TrimOneTrailingNewline`). 반증 확인 완료.
      **핵심 계약**: 셀 안에서 `\par`는 문맥에 따라 내용(소프트 개행)이거나 구조(문단 경계)다 —
      깊이가 바뀌는 자리의 것은 구조다.
- [x] **공허해진 테스트 1건 정정** — `RtfExportFlattensInlineTableInsideCell`이 리라이트 후
      이름·전제가 거짓인 채 통과하고 있었다(텍스트 존재만 단언). **교훈**: 동작을 바꿀 때 그 동작을
      단언하던 테스트가 *여전히 통과한다면* 무엇을 단언하고 있는지 다시 볼 것.
- [x] 축 교차 신규: 3단 중첩(`\itap3`) · 병합 셀이 중첩 표를 품은 경우 · 가로+세로 동시 병합 ·
      종합 문서 **2회 왕복**(위 결함을 잡음) · 손상 입력 6종(중첩 절단·`\itap` 레벨 점프·셀 없는 행·
      셀 중간 깊이 하강·`\cellx` 없는 `\cell`·음수 경계).
> **왜 2회 왕복인가**: 1회 왕복으로는 안 보이는 증식 계열이 이 코드베이스에서만 두 번 나왔다
> (HTML 인라인 표 뒤 공백, RTF 중첩 표 앞 개행). 왕복 테스트는 기본 2회로 쓸 것.

## 3차 전수조사 (2026-07-31) — 완료
앞선 두 패스가 훑은 곳을 반복하면 수확이 낮으므로, **이번 세션이 바꾼 것과 안 바꾼 코드의 접촉면**만
겨냥했다. 결함 2건(같은 뿌리) — 둘 다 **이 세션이 만든 것**이다. 테스트 101/101.

- [x] **큰 줄간격에서 세로 이동이 깨지던 회귀** — 캐럿을 baseline 기준·글자 높이로 바꾼 수정의 파생.
      세로 이동은 "캐럿에서 넛지"로 다음 줄을 찾는데 캐럿이 더는 줄 상자가 아니라, 넛지가 같은 줄 안에
      떨어졌다. 아래쪽 어긋남 `0.2×(줄상자−글자높이)`, **위쪽 `0.8×(…)`로 4배** — 위쪽은 300%에서 ~26px라
      셀 내 이동이 실패하고 **셀 탈출 단계로 빠졌다**(사용자 리포트: "맨 아랫줄에서 ↑ 하면 표 밖으로").
      `CaretInLayout`/`CaretToDocPoint`가 `LineTop`/`LineBottom`을 함께 돌려주고 이동만 그것을 쓴다.
      > **핵심 계약**: 세로 이동 probe는 **캐럿이 아니라 줄**을 넘어야 한다(위=`LineTop`, 아래=`LineBottom`).
      > **캐럿 기하를 건드리면 세로 이동 probe를 반드시 함께 볼 것** — 이 하나의 변경에서 결함이 셋
      > 파생됐고(아래쪽·위쪽·최초 정렬), 100%에서는 세 값이 다 일치해 **기본 설정에선 증상이 안 나온다**.
      > 줄간격 200/300%는 캐럿·이동을 만질 때마다 확인할 것.

### 근거로 해소(수정 불요) — 재조사 방지
- **플래그 9개 DP 전환의 스레드 안전성**: 코드베이스에 `ConfigureAwait(false)`가 **하나도 없고**
  `Task.Run` 본문은 정적 포매터만 호출(캡처된 지역변수만) → DP 읽기는 전부 UI 스레드.
- `Status.ParagraphDocOrigin`은 `docCaret − inLayoutCaret`이라 양쪽이 같은 값을 써서 상쇄된다.
- `ScrollCaretIntoView`·드롭 프리뷰는 캐럿의 **시각 범위**를 쓰는 것이 맞다(캐럿을 보이게 하는 목적).
- `MovePage`는 뷰포트 크기 스텝이라 캐럿/줄 차이가 무의미. "눌린 방향으로 움직였는가" 가드는
  캐럿 Y끼리 비교라 기준 일관.
- 툴바 아이콘 중복 4쌍(이미지 삽입/교체 등)은 **서로 다른 메뉴 + 라벨 있음** → 충돌 아님.
  충돌은 찾기/줌처럼 **라벨 없이 인접**할 때만 생긴다.

## 랜덤 편집열 퍼즈 도입 (2026-07-31) — 완료
정독·상류 대조·왕복 테스트를 다 소진한 뒤 남은 유일한 검출 수단. `tests/.../DocumentFuzzTests.cs`,
모델 레벨이라 헤드리스. 24시드 × 300스텝, 매 스텝 구조 불변식 + 네 포맷 2회 왕복 멱등성.
**결함 3건 적발**(상세는 CHANGELOG): 셀 안 다중 문단이 RTF 왕복마다 병합 · HTML `<br>` 구분자가
내용이 되어 증식 · **들여쓴 목록만 있는 문서가 HTML 왕복에서 전체 마크업 텍스트로 변질**(실데이터 손실).
- **왜 이것만 찾았나**: 셋 다 "코드는 읽혔는데 그 축의 다른 값을 안 넣어 본" 경우다. 정독은 각 조각이
  개별적으로 맞기 때문에 못 본다. 이 세션의 결함 상당수가 같은 성격이었다(인라인 표 위치, 왕복 2회째,
  줄간격 300%).
- **운용 규칙**: 시드 24는 CI 예산이다. **포매터를 건드리면 시드를 넓혀 돌릴 것** — 위 결함 중 하나는
  200시드에서만 나왔다.
- [x] ~~**알려진 미수정**: 시드 ≥27에서 병합 셀 안 공백 하나가 2회째 HTML 왕복에서 소실~~ →
      **2026-08-06 전수조사에서 수정**(위 절). 당시 유력하다고 적어 둔 방향("인라인 경로의 가드를 블록
      경로에도 맞춘다")은 **그대로 하면 틀렸다** — 인라인 경로는 공백을 즉시 넣는데, 블록 경로에서 그러면
      문단 끝에 공백이 쌓인다(실패 4 → 12건으로 실측). 미루는 판이어야 한다.

## 1.0.0 릴리스 — 완료 (2026-07-31 게시)
- [x] **`<Version>` 1.0.0** + `PackageReleaseNotes` 재작성(breaking 1건 + 동작 변경 2건을 앞세움).
- [x] **`PublicAPI.Unshipped.txt` → `Shipped.txt` 이관** — 552줄로 동결, Unshipped 비움.
- [x] **CHANGELOG `[1.0.0]` 절** — 맨 앞에 "이 버전으로 올릴 때 확인할 것"(breaking/동작 변경/SDK 하한).
- [x] **README** 상태 배지 → 1.0 동결 + SemVer, stale 문구 정정(테스트 수 26→91, "API may still change" 제거).
- [x] **AOT 게시 재검증(실측)** — self-contained `win-x64.pubxml` 성공: 총 **87MB**, 네이티브 exe
      **15.1MB**, `coreclr.dll`·`clrjit.dll`·`System.Private.CoreLib.dll`·관리형 `WinUIRichEditor.dll`
      전부 부재(= 진짜 AOT). **실행 + Win2D 렌더 확인**(PrintWindow 캡처 — 제목·서식·링크·인용·리스트·
      툴바 아이콘 정상).
- [x] **`dotnet pack` 실물 검사** — 패키지 = `dll` + **`xml`**(신규) + `README` + 메타데이터.
      분석기·`PublicAPI.*` 미포함 확인.
- [x] **NuGet 소비자 스모크 — 통과(13/13)**. 로컬 feed + `nuget.config` + **`PackageReference`**(ProjectReference
      아님)로 별도 unpackaged WinUI 3 앱을 세워 빌드·실행. 1.0의 업그레이드 위험을 겨냥해 확인:
      **breaking**(`RoundTripHarness`가 배포 어셈블리에 없음) · **동작 변경**(`LoadJson`/`LoadPackageAsync`가
      손상 입력에 예외를 던지고, 그때 **열려 있던 문서가 보존됨**) · **신규 DP**(`SetValue`로 설정 가능) ·
      `FocusEditor()` · HTML/RTF 왕복 · 중첩 표 `GetPlainText` · XML 문서가 복원된 패키지에 존재.
      **재현 레시피**(스캐폴드는 임시 폴더라 매번 사라진다): 데모 csproj를 본떠
      `WindowsPackageType=None` + 메타패키지 `Microsoft.WindowsAppSDK` + `WinUIRichEditor` PackageReference,
      `App.xaml`은 `XamlControlsResources`만, 체크는 `OnLaunched`에서 **async로** 돌리고 결과를 파일로 쓴다.
      ⚠️ 함정 3가지(실제로 다 밟았다): ① 비동기 API를 UI 스레드에서 `.GetResult()`로 블록하면 **데드락**
      (continuation이 같은 스레드를 필요로 함) — 소비자도 반드시 `await`할 것 ② `DispatcherQueueTimer`를
      지역 변수로 두면 울리기 전에 GC됨 ③ `FocusEditor()`는 **레이아웃 완료 전 `false`** — 실패가 아니라
      WinUI가 Focus()를 무시하는 것.
- [x] **`v1.0.0` 태그 푸시** → 워크플로 success, nuget.org 인덱싱 확인, GitHub Release 작성 완료.

### 구조적 잔여 (릴리스 차단 아님, 가장 큰 항목)
- **테스트 인프라 격차**: 포트 90 vs 원본 528 유닛 + 17 렌더 + 상호작용 하네스. 이번 세션의 결함 중
  컨트롤·상호운용 계층은 **전부 사람이 실기로** 잡았고 자동 검증이 하나도 없었다. 원본은 그 격차를
  `InteractionHost`(헤드리스 Window에 실제 클릭·드래그·키 주입)와 렌더 픽셀 테스트로 메웠다.
  WinUI는 DP 정적 생성자가 런타임을 요구해 더 어렵지만, **포매터·모델 계층은 헤드리스가 되므로**
  (트랙 A가 그 증거) 거기부터 세울 수 있다. 랜덤 편집열 퍼즈도 모델 레벨이면 가능하다 — 원본에서
  "목록 켜기가 인라인을 복제"를 찾아낸 것이 그 퍼즈다.
- **원본 백로그와 공유하는 항목**(원본에도 없음): 표 행/열 조작의 에디터 레벨 공개 API,
  `RichEditorShortcuts` 공개.

## 상류 1.0.0 대조 (2026-07-31) — 완료 (트랙 A~E)

원본 AvaloniaRichEditor가 **1.0.0**을 냈다(API 동결). 새 기능이 아니라 **검증 깊이**로 끊은 릴리스다 —
`InteractionHost`(헤드리스 Window에 실제 클릭·드래그·키 주입), 렌더 픽셀 9→17, 전 소스 감사 라운드 3~7,
랜덤 편집열 불변식 퍼즈(20시드×300스텝), Word/HWP/브라우저 **실물 육안검증**, 유닛 **355→528**.

**대조 기준점**: 양방향 수렴은 2026-07-25 원본 `bcc12e4`까지였다. 그 뒤 원본은 `c5a3b02`(라운드3)~
`08f1a11`로 **약 35커밋**을 더 갔고, 그 구간이 미대조 영역이다. 아래는 그것을 포트 소스에 1:1로
확인한 결과다. **방침(사용자 결정)**: 포트 쪽에 특별한 장점이 없으면 **원본에 맞춘다**.

### 트랙 A — 포매터 계층 ✅ 완료 (2026-07-31)
빌드 0/0, 테스트 **82/82**(신규 8). 상세는 CHANGELOG "상류 1.0 대조 — 트랙 A".
- [x] HTML `colspan` 상한(메모리 고갈 DoS) · `.flow`/JSON 선언 표 크기 선할당 제거 + 열 수 상한 ·
      JSON `null` 항목(Blocks/Cells/Inlines) NRE
- [x] 손상 문서 계약을 **예외로 통일**(3차의 "빈 문서 반환" 결정을 뒤집음 — 상류 논거 채택) ⚠️ 동작 변경
- [x] RTF 가져오기 3건: 무시 가능 그룹 앞 텍스트 소실 · 건너뛰는 그룹 안 `\trowd`/`\cell`/`\row`/`\cellx`가
      부모 셀 폐기 · `{\nonesttables}` 폴백 중복
> **반증 확인 완료**: 신규 8건 중 6건은 수정 전 FAIL, DoS 2건은 **테스트 프로세스가 정지**하는 것을
> 실측했다(8분 타임아웃). 로드맵의 "무해 판정은 반증 테스트로 확인할 것" 규칙 적용.

### 트랙 B — 컨트롤 계층 ✅ 완료 (2026-07-31, 실기 검증 완료)
빌드 0/0, 테스트 82/82. 상세는 CHANGELOG "트랙 B".
- [x] **목록 켜기가 non-Run 인라인을 복제**(`SplitByNewlines`) → **이동**으로. 인라인 표 셀 안 캐럿이
      문서에서 떨어진 트리를 가리키던 것(타이핑이 안 보이는 곳으로 감).
- [x] **표 셀 안 블록 이미지를 선택조차 할 수 없었다** — 원본은 "핸들이 안 먹는다"였지만 포트는
      **핸들 자체가 없었다**(셀 이미지가 블록 레이아웃 맵에 없어 클릭이 안 닿음). `_cellImageRects`
      레지스트리 신설(그려진 rect 적재) + 드래그를 **그려진 크기** 기준으로 + 리사이즈 시 조상 표 체인
      evict. **핵심 계약**: 핸들 그랩 밴드는 자기 rect 밖으로 나가므로 `TryBeginImageInteraction`에서
      **모든 핸들 검사가 모든 선택 클릭보다 먼저** 와야 한다.
- [x] **인라인 표 셀 안의 블록이 삭제되지 않았다**(`RemoveBlockFromCells`가 블록 표만 재귀).
      위 수정이 이 경로를 도달 가능하게 만들어 함께 고쳤다 — `TextRange.BlockHolds`(2026-07-25)와
      **같은 계열의 다른 사본**이다. ⚠️ 이런 워커는 사본이 더 있을 수 있다.
- [x] **`GetPlainText()`/`GetImageCount()` 1레벨 → 재귀**(보조기술 가시성 / 소프트 리밋 정확도).
- [x] **`Language` 스레드 마샬링**(`DispatcherQueue`) — 백그라운드에서 바꾸면 툴바가 죽던 것.
> **자동 검증 없음**(전부 컨트롤 레벨 — DP 정적 생성자가 WinUI 런타임을 요구). 82/82는 회귀 없음만
> 말해준다. **실기 검증 완료(2026-07-31, 사람 확인)**: ① 인라인 표/이미지가 든 문단의 글머리표 토글 후
> 표·이미지 잔존 + 셀 안 타이핑 표시 ② 셀 이미지 클릭 선택 → 핸들 드래그로 실제 크기 변경 + 행 즉시
> 성장 ③ 셀 이미지 Delete ④ 인라인 표 셀 안 이미지도 동일 ⑤ 최상위 이미지 리사이즈 회귀 없음 — 5건 모두 정상.

### 트랙 C — 툴바 포커스 ✅ 완료 (2026-07-31, 실기 검증 완료)
**사용자 실기 리포트로 결함 확정**("툴바 버튼 누른 뒤 타이핑이 안 된다"). 예측이 맞았다 — 포커스 보장이
한 줄도 없었다. 빌드 0/0, 테스트 82/82. 상세는 CHANGELOG "트랙 C".
- [x] **(a) 스트립의 어떤 것도 포커스를 안 가져간다** — `AllowFocusOnInteraction=false`를 버튼/토글
      팩토리에 + 빌드 시 논리 트리 워크로 **호스트 `LeadingItems`/`TrailingItems`까지**.
- [x] **(b) 열려 있는 동안 필요한 것은 닫을 때 반환** — 콤보는 `DropDownClosed`(`SelectionChanged`가
      **아니라** — 열린 목록을 화살표로 훑는 중에 포커스를 뺏는다), 피커 팝업 4종은 `Closed`.
      콤보는 전부 `MakeCombo` 한 곳을 지나므로 페이지/줌 콤보도 자동 커버된다.
- [x] 줄간격 입력 상자는 편집 필드라 포커스를 받되 **Enter=완료**로 반환. 커밋이 언두를 쌓으므로
      Enter가 유발하는 `LostFocus` 중복 커밋을 가드 — **Enter 1회 = 언두 1스텝**.
- [x] 신규 공개 API **`RichEditor.FocusEditor()`**(캐럿 불변). 포커스는 내부 캔버스에 있고 컨트롤은
      탭 스톱이 아니라, 호스트가 컨트롤에 `Focus()`를 불러도 안 닿는다. **핵심 계약**: 포커스를
      가져가는 새 크롬을 추가하면 반드시 이걸로 돌려줄 것.
> **자동 검증 없음**(컨트롤 레벨). **실기 검증 완료(2026-07-31, 사람 확인)**: ① 버튼 클릭 후 즉시
> 타이핑 ② 콤보 화살표 탐색 중 포커스 유지 + 선택 후 타이핑 ③ 피커 팝업 4종 닫은 뒤 타이핑
> ④ 줄간격 Enter → 적용 + 복귀 + **Ctrl+Z 1회로 되돌림**(언두 가드 동작) ⑤ View 파일 액션 —
> 5건 모두 정상.

### 캐럿 baseline 정렬 (2026-07-31, 사용자 리포트 — 트랙 대조와 무관한 포트 자체 결함)
- [x] **캐럿을 줄 상자가 아니라 그 줄의 baseline 기준으로.** 규칙이 두 개(커스텀 간격=줄 상자 바닥,
      자연 간격=줄 상자 전체)라 둘 다 글자보다 낮았다. 하나로 통일 + 빈 줄도 동일 규칙 +
      `BaselineFraction` 상수화. **핵심 계약**: DirectWrite에 지정하는 baseline 위치와 거기서 글리프
      상단을 역산하는 비율은 **같은 상수**여야 한다. 큰 인라인 오브젝트 줄만 예외(오브젝트 baseline =
      자기 바닥이라 바닥 정렬이 곧 baseline 정렬).
      **재발 방지**: 줄 상자(`LayoutBounds`)는 글리프 박스가 아니다 — 세로 배치는 `LineMetrics.Baseline`
      에서 출발할 것. 리스트 마커(2026-07-16)와 캐럿(오늘)이 같은 함정에 두 번 빠졌다.
> **실기 검증 완료(2026-07-31, 사람 확인)**: 100/200/300%, 빈 줄·Shift+Enter, 제목, 이미지·표 줄,
> 표 셀 안 캐럿 모두 정상.

### 트랙 D — 상호운용 갭 ✅ 완료 (2026-07-31, 실기 검증 완료)
빌드 0/0, 테스트 **86/86**(신규 4, 3건은 수정 전 FAIL 확인). 상세는 CHANGELOG "트랙 D".
- [x] **HTML 인라인 표 3중 결함**: `data-are-inline` 마커로 인라인 복원(기존엔 블록 표로 돌아와 호스트
      문단 영구 분할) + `display:inline-table`+자기 열 너비(기존 `width:100%`라 전폭 밴드) +
      인라인일 때 `</table>` 뒤 개행 제거(왕복마다 공백 1칸 증식).
- [x] **RTF 인라인 표**: 무시 가능 `{\*\arinline}` 마커 — 외부 앱은 종전대로 블록 표, 자체 리더만 복원.
- [x] **RTF 셀 배경**(`\clcbpat`) 쓰기+읽기. 검정 배경은 `blackIsDefault: false`(형광펜과 동일 이유).
      ⚠ `BuildRowDefinition`을 static에서 인스턴스로 바꿨다 — 색을 문서 색상표에 등록해야 하므로.
- [x] **RTF 중첩 표** — 아래 "트랙 D 마무리" 절에서 처리 완료(실기 검증 후 착수한다는 순서 근거대로).
- 이미 있음: `\clbrdr*` 테두리, 세로 병합 `\clvmgf`/`\clvmrg` 쓰기+읽기.
> **실기 검증 완료(2026-07-31, 사람 확인 — Word·HWP·Edge·자체 왕복 5종 비교 캡처)**:
> **이번 수정 3건 전부 동작 확인.** ① 브라우저에서 인라인 표가 텍스트 줄 안에 앉음(전폭 밴드 아님)
> ② RTF 저장→불러오기에서 인라인 표가 **인라인으로 복원** ③ 같은 경로에서 **셀 배경색 유지**.
> Word 붙여넣기는 중첩 표·셀 배경·세로/가로 병합 모두 정상.

### 트랙 D 마무리 — RTF 표 모델 ✅ 완료 (2026-07-31, HWP 실기 검증 완료)
실기에서 드러난 잔여 2건은 같은 코드를 건드리므로 함께 처리. 빌드 0/0, 테스트 **90/90**(신규 4).
- [x] **파서 셀이 블록을 담도록**(`List<List<Paragraph>>` → `List<List<TableCell>>`). 두 항목의 선행 조건.
- [x] **중첩 표 실제 `\itapN`+`\nestcell`/`\nestrow`** 양방향(임의 깊이, `\itap`으로 레벨 구분).
      **핵심 계약 2가지**: ① 중첩 표에 하강하기 전 `\par`로 앞 문단을 닫을 것(안 닫으면 Word가 부모
      텍스트를 첫 중첩 셀에 붙인다) ② 중첩 표가 끝나면 셀이 자기 `\itap`을 다시 선언할 것(안 하면 뒤
      문단이 닫힌 표에 들어가 Word가 버린다). 남은 손실: 중첩 표 열 너비(`{\*\nesttableprops}` 안).
- [x] **가로 병합을 기하 형태로**(병합 구간 = 셀 1개 + `\cellx`가 오른쪽 끝). 리더는 행별 `\cellx`
      합집합으로 격자를 만들어 colspan 역산 + **Word 플래그 형태도 계속 읽음**.
      세로 병합은 기하 표현이 불가능해 `\clvmgf`/`\clvmrg` 유지(HWP도 이쪽은 정상이었다).
      **문서화된 한계**: 모든 행이 똑같이 병합되면 격자 단서가 없어 한 열로 접힌다(렌더링은 동일).
      테스트로 고정해 뒀다.
> **실기 검증 완료(2026-07-31, 사람 확인 — 원본/HWP/Word/Word→에디터 캡처)**:
> ① **HWP 가로 병합 유지 — 가설 적중, 해결됨** ② HWP 중첩 표는 여전히 격자 아님(아래 참조)
> ③ 중첩 표 앞뒤 문단 순서 Word에서 정상 ④ **Word 붙여넣기 회귀 없음**(병합·중첩·세로병합·셀배경 전부)
> ⑤ **Word 문서 → 에디터 회귀 없음**(중첩 표가 격자로 들어옴, 열 너비만 기본값 = 문서화된 손실)
> ⑥ 자체 RTF 왕복 정상.

#### HWP는 RTF 중첩 표를 구현하지 않는다 (포트 결함 아님 — 재조사 방지)
HWP 캡처에서 중첩 셀이 **구분자 없이 이어붙는다**(`중첩1중첩2`). 즉 HWP는 `\nestcell`을 모르는 제어어로
버리고 중첩 셀 텍스트를 부모 셀의 이어지는 글로 읽는다. 우리가 쓰는 구성(`\nestcell` +
`{\*\nesttableprops …\nestrow}{\nonesttables\par}`)은 **원본 AvaloniaRichEditor 1.0과 바이트 단위로 동일**
하므로(소스 확인함) 원본을 HWP에 붙여넣어도 결과가 같다. Word는 이 구성을 정확히 읽는다.
- **가능한 절충(미적용, 실수요 시 판단)**: `\nestcell` 바로 앞에 `\tab`을 넣으면 HWP에서 `중첩1→중첩2`로
  최소한 읽히지만, **Word의 중첩 셀 끝에도 탭 문자가 들어간다**. 현재 Word 출력이 완벽하므로 더 중요한
  타깃을 오염시키지 않는 쪽을 택했다. 바꾸려면 이 트레이드오프를 의식적으로 받아들일 것.
- HWP로 표 구조를 온전히 보내야 한다면 **중첩 표를 쓰지 않는 것**이 현실적인 회피책이다.

### ~~트랙 D 후속 — 실기에서 드러난 잔여 2건~~ → 위에서 처리 완료
- [x] **HWP에서 가로 셀 병합이 풀린다**(신규 결함, 사용자 캡처). **Word는 정상**이므로 HWP 전용이다.
      현재 writer는 병합을 Word식 플래그 형태로 쓴다 — 열마다 `\cellx`를 하나씩 두고 앵커에 `\clmgf`,
      피복 슬롯에 `\clmrg`. HWP는 이 형태를 안 따르는 것으로 보이고, **가설**은 기본 RTF 표 모델
      (병합된 칸은 셀을 하나만 쓰고 그 `\cellx`를 병합 오른쪽 끝에 둔다)을 기대한다는 것이다. Word는
      그 형태도 읽으므로 그쪽으로 통일하는 것이 양쪽을 만족시킬 가능성이 높다 — **다만 검증 없이는
      가설이다**(HWP 실기 확인 필수). 세로 병합(`\clvmgf`/`\clvmrg`)은 HWP에서도 정상이었다.
      ⚠️ 이걸 바꾸면 **리더도 함께** 바꿔야 한다: 지금은 `\clmgf`/`\clmrg` 플래그로 span을 복원하는데,
      넓은 `\cellx` 형태에서는 **행별 `\cellx` 경계의 합집합**에서 열 격자를 만들고 각 셀이 덮는 경계
      수로 colspan을 역산해야 한다(현재 `FinalizeTable`은 첫 행의 cellx만 보관한다).
- [x] **RTF 중첩 표** — 위에서 처리 완료.
> **두 항목은 같은 코드(표 모델)를 건드리므로 함께 하는 것이 맞다.** 리더의 셀 모델을
> `List<List<Paragraph>>` → 블록 보유 셀로 바꾸는 리팩터가 두 작업의 공통 선행 조건이다.
> **인라인 표가 Word/HWP에서 줄에서 분리되는 것은 결함이 아니다** — RTF에 인라인 표 개념이 없어
> 호스트 문단을 쪼개는 구조이고, 원본 1.0도 동일하다(자체 왕복만 마커로 복원). HTML 경로는 브라우저에서
> 줄 안에 앉는 것으로 확인됐다.

### 트랙 E — API 표면 ✅ 완료 (2026-07-31)
빌드 0/0, 테스트 90/90, `dotnet pack` 정상(패키지 = dll + xml + README, 분석기·API 파일 미포함 확인).
- [x] **`RoundTripHarness`를 데모로 이관 + `internal`**(⚠️ breaking). 포트에서는 **아무도 참조하지 않는
      죽은 공개 API**였다. 데모 `--roundtrip=<dir>`로 실행(WinExe라 stdout이 없어 report 파일로 씀).
- [x] **`AutoLinkOnType` → 의존성 속성**(`AutoLinkOnTypeProperty`). 이름·기본값 불변이라 호환 영향 없음.
- [x] **`PublicAPI.Shipped.txt`/`Unshipped.txt` 도입** — 공개 표면 **543개** 기준선. 이제 공개 API 변경이
      빌드 경고 + diff로 드러난다. 기준선 생성법(재사용):
      `dotnet format analyzers <csproj> --diagnostics RS0016 --severity warn` → Unshipped에 전량 생성 →
      릴리스된 것만 Shipped로 승격. 이번 세션 델타는 Unshipped에 남겨 뒀다(`FocusEditor`·
      `AutoLinkOnTypeProperty` 추가, `RoundTripHarness` 제거).
- [x] **패키지에 XML 문서 포함**(`GenerateDocumentationFile`) — 켜자 **끊어진 cref 4건**이 드러나 함께
      수정. **교훈**: 문서 생성을 끈 채로는 XML 주석의 링크가 썩어도 아무도 모른다.

### 대조 결과 "이미 있음 / 해당 없음" (재조사 방지)
**이미 갖춤(소스 확인)**: 선택 인식 문단 명령 6종(`SelectedParagraphs()`가 셀 블록까지) · 셀 블록이 실제
명령 대상 · 단계 Ctrl+A · 셀 목록 마커 · `CompareTo`/`BlockHolds` 재귀 · `MergeCells` 다중 블록 ·
`CloneFormat`/`CopyFormatFrom` · 리사이즈 no-op 언두(`PushDragUndoOnce`) · 문서 끝 Backspace/Delete
no-op 언두(**검증 후 PushUndo**) · Enter 단일 체크포인트 · HTML `font-weight` · RTF 셀 그림 탈출 ·
인라인 표 셀 정규화 · 원격 이미지 게이트 전 경로 · 동기 `ParseHtml` 무네트워크 · 찾기 현재 매치 제외 ·
Ctrl+Shift+X · 수식키 단독 무시 · **읽기 전용 Tab이 트랩 아님**(명령별 게이트라 Tab이 안 잡힌다) ·
`HitTestBlockList`의 인라인 표 하강 · 히트테스트 오프셋 클램프 · `FindCell` 부모 체인 · ↑/↓ 표 진입.

**아키텍처상 무관**: 블록 캐럿 3건(포트엔 `_caretBlock`이 **없다**) · IME 3건(포트는 조합 텍스트를
**모델에** 넣으므로 측정·히트테스트·행 성장이 자동 정합. 원본은 렌더 오버레이라 3번 따로 고쳐야 했다) ·
셀 선택 모드가 문서 교체·화살표에서 stale(포트는 선택 끝점에서 **무상태 유도**) · 마우스 이동당
`new Cursor()`(포트는 `ProtectedCursor` 공유).

**구조적 격차**: 원본 528 유닛 + 17 렌더 vs 포트 82. 위 결함군은 원본에서 대부분 *퍼즈·상호작용 테스트·
악성 입력 투입*이 찾아냈고 포트엔 그 셋이 없다. 포매터·모델 계층은 WinUI 런타임 없이 헤드리스로
가능하므로(트랙 A가 그 증거) 거기부터 세울 수 있다.

## 보류 / 백로그 (backlog)
실수요가 생기면 그때 꺼내 쓸 항목. **파리티 갭 아님**(원본 AvaloniaRichEditor에도 없음) — 순수 net-new.
- **암호화 `.flow` 저장** — 지금 구현 안 함(수요 근거 없음, 엔진 핵심 아님). 만들게 되면 *제대로* 할 것:
  현대 KDF(Argon2id 등, PBKDF2는 앵커하지 말 것), 평문 `.flow`와 공존하는 외부 봉투 방식, 비밀번호
  분실 시 복구 불가 경고, 편집 중 평문이 UI/임시파일로 새지 않게. **어중간하게 넣어 "암호화됐다"는
  잘못된 안심을 주는 건 금지.** (우회로: 7-Zip AES / BitLocker·VeraCrypt로 `.flow`를 감싸면 됨.)

## 메모
- 원본 경로: `C:\Users\centw\source\repos\AvaloniaRichEditor`.
- 포팅 사전·핵심 규칙은 `CLAUDE.md` 참조.

<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="WinUIRichEditor.Demo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WinUIRichEditor.Demo"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="WinUIRichEditor.Demo"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="WinUIRichEditor.Demo">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <!-- Top-level demo navigation: the four library layers. -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="4" Padding="8,4" Background="#F3F3F3">
            <Button x:Name="NavControl" Content="컨트롤" Click="OnNavControl" ToolTipService.ToolTip="RichEditor 단독 (편집)" />
            <Button x:Name="NavReadOnly" Content="읽기 전용" Click="OnNavReadOnly" ToolTipService.ToolTip="RichEditor 읽기 전용 (IsReadOnly=true)" />
            <Button x:Name="NavToolbar" Content="컨트롤+툴바" Click="OnNavToolbar" ToolTipService.ToolTip="RichEditor + RichEditorToolbar" />
            <Button x:Name="NavView" Content="View" Click="OnNavView" ToolTipService.ToolTip="RichEditorView (툴바+페이지/줌 크롬+상태바+파일 액션)" />
        </StackPanel>

        <Frame x:Name="RootFrame" Grid.Row="2" />
    </Grid>
</Window>
